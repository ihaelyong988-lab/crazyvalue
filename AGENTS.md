# AGENTS.md — crazyvalue 저장소 작업 규칙

> 작업 재개 순서: 이 파일 → `docs/PLAN.md`(기획안 v3.1 사본, 원본은 기획 폴더 `경매 최저가/`) → `docs/TRACE.md`(실행 완전성 원장) → 마지막 Phase 보고.
> 상위법: `~/.claude/CLAUDE.md` 반복금지 헌법 · 기획 폴더 `경매 최저가/AGENTS.md`.

## 0. 재개 지점 — 2026-08-06 이후 (해소되면 이 절을 통째로 삭제한다)

> 2026-08-05 저녁에 미해소 10건 중 **7건을 닫았다**. **서비스 정상 가동** — typecheck 0 · 유닛 131/131 · E2E 19/19 · UI 게이트 0건 · 데이터 가치 게이트 0건. 남은 것은 아래 3건이 전부다.

**남은 것 (3건, 순번은 옛 10건 표를 승계 — 재번호 금지)**

| # | 남은 것 | 처방 |
|---|---|---|
| 2 | **"매일 새벽 3시"의 실발화는 03:53~04:28**(스케줄 런 10건 실측 · 지연 53~88분). cron 지연 발화는 플랫폼 정상 동작이라 정시 보장 수단이 없다. 슬롯 값은 건드리지 않았고 §1에 실측을 병기했다 | **주인님 결정** — 지금 창을 수용할지, 슬롯을 앞당겨 체감 시각을 맞출지(앞당기면 03:00 이전 발화가 생긴다) |
| 3 | **모드 분기 로직이 스케줄 이벤트에서 아직 0회 실행.** `github.event.schedule` 주입 자체는 반증됐다 — 런 30943387521 로그가 `0 18 * * 0-5`를 그대로 출력한다. 미검증인 것은 분기 게이트 교체 **이후**의 스케줄 런이다 | 2026-08-05 18:00Z(KST 08-06 03:00) 슬롯 로그의 `슬롯 '0 18 * * 0-5' · 모드 quick` 한 줄로 확정된다. **그전까지는 미검증이 정확한 상태다** |
| 10 | 취소된 대기 런이 조용히 그날을 건너뛴다(구조적·미관측) | 관측되면 concurrency 정책 재검토 |

**#1에 붙인 가정 — 주인님 확인 대상.** "신규 갯수"는 라벨을 바꾸는 대신 **집계가 라벨의 뜻을 만족하도록** 고쳤다(§1 · `countNewOnSharedDates`). 확정 표기 `[새로 갱신] … · 신규 N건`을 건드리지 않는 쪽을 택한 것이다. 반대 방향(라벨을 실측값에 맞춤)을 원하시면 되돌린다.

**닫은 것(2026-08-05 저녁, 7건)**: #1 신규 집계 정정 + 정의 문서 3곳 정합 · #4 날짜 라벨 KST 전환 4곳 · #5 TRACE Phase 표 현행화 · #6 `docs/OPERATIONS.md` 신설(README:40 참조 성립) · #7 gen-mock 주석 · #8 마커를 읽는 Stop 게이트 연결 · #9 §9 TZ 조문에 적용 범위(로컬 셸 한정) 명시.

**잘라낸 것**: 방문자 여정 감사·적대적 반증·완결성 비평 — 주인님 "신속" 지시로 범위 밖. 필요하면 별도 라운드로 연다.

**다음 라운드 후보(증거 있음, 이번 범위 밖)**: `docs/PLAN.md:542·583`의 "주간 점검" 표현이 매일 갱신과 어긋남 · TRACE `§4.5`·`§13 규칙 15` 표의 "대기" 다수가 같은 문서 뒷절과 불일치 · 신규 집계의 잔존 오차(직전 런에서 `OUTPUT_CAP` 배분 경계에 잘렸다가 이번에 편입된 물건은 신규로 계상됨) · Phase 5 실물 잔여(릴리스 태그 미생성 · `@vercel/analytics` 미도입).

**별건(오늘 범위 밖)**: 주간 full 수집이 매번 로봇탐지로 조기 종료 — 이슈 #6·#7 열린 상태. 실취득 유찰 이력이 6%에 그치는 원인.

## 1. 정체성·확정 결정 (재질문 금지)
- **CrazyValue(미친가치)** — 유찰 2회 이상 법원경매 물건만 큐레이션하는 모바일 온리 PWA.
- 스택: Next.js 15 + TypeScript + Tailwind v4 · 정적 JSON(시도 17분할) · **GitHub Actions 매일 03:00 KST 갱신**(2026-08-05 주인님 확정 — 슬롯 2개는 실행 여부가 아니라 모드를 가른다: `0 18 * * 6`=KST 일요일 full 전량 · `0 18 * * 0-5`=KST 월~토 quick 경량. full은 요청 수가 로봇탐지 임계에 닿아 매번 조기 종료하므로 주 1회로만 돌린다) · Vercel 자동 배포.
- **발화 시각은 지정값과 실측이 다르다** — 슬롯 지정은 18:00Z(=03:00 KST)이지만 **실발화는 03:53~04:28 KST**다(정기 슬롯 10회 실측 18:53:20Z~19:27:48Z · 지연 53~88분). GitHub cron의 지연 발화는 플랫폼 정상 동작이라 슬롯 시각으로 정시를 보장할 수단이 없다 — 앞당기면 창이 통째로 이동할 뿐이다. 슬롯 값 조정 여부는 주인님 결정 사항이라 현행 유지한다.
- **갱신 파이프라인에 수동 개입 경로를 두지 않는다(2026-07-27 주인님 지시).** 실패·드롭은 다음 날 슬롯이 스스로 이어받는다. "안 되면 수동 실행"은 해법이 아니다. (단, 파이프라인 코드 수정 직후의 실전 등가 검증 1회는 `workflow_dispatch`로 당일 수행한다 — §9 [2026-07-30].)
- **산출물 = 유효·중복제거 후 최대 1,000건**(`OUTPUT_CAP`, 2026-07-30 주인님 확정). **선별은 임박순 절단이 아니라 앞으로 `OUTPUT_WINDOW_DAYS`(7일)의 매각기일 배분**(2026-08-02 주인님 승인 — 임박순 절단은 상한을 첫 기일에서 소진해 나머지 날을 전부 기일 경과로 만든다). 배분 창은 갱신 주기(`nextUpdateAt`)에서 떼어내 상수로 고정한다(2026-08-05) — 매일 갱신이면 "다음 갱신일까지"가 하루라 창이 붕괴한다. 기일 내 우선순위는 저가(`priceRatio`) 순. 기준은 /guide에 공개. 게이트는 유효 부분집합 상시 배포, 기각은 유효 0건일 때만.
- **리프레쉬(2026-08-02 주인님 지시)** — 홈 옆 탭. 탭한 시점부터 다시 수집해 갱신한다. `POST /api/refresh` → `workflow_dispatch(mode=quick)`. **경량 모드 = 매각기일 창 7일 + 상세 생략**(`--window 7 --no-detail`), 실측 182초·요청 162회. **쿨다운 1시간**(직전 실행 기준, 2026-08-02 주인님 확정 — 초기 6시간은 실측이 아닌 보수적 추정이었고 "간단히 갱신" 목적을 해쳤다. 실측: 전량 크롤 직후엔 세션 거부였으나 13분 뒤 경량 크롤은 완주). 진행 중·쿨다운은 서버가 거절한다. **정기 슬롯 2개(일 full · 월~토 quick)는 그대로 유지** — 리프레쉬가 대체하지 않는다. 실행 토큰은 서버 환경변수 `GH_DISPATCH_TOKEN`이며 응답에 싣지 않는다(미설정 시 상태는 보이고 실행만 막힌다 — 저장소가 공개라 조회는 무인증).
- **홈 기준일 바 표기(2026-08-05 주인님 확정)** — `[새로 갱신] 08-03(월) 19:58 · 신규 42건`. "새로 갱신" 배지는 상시 노출이고(신선도 조건부 아님), 오른쪽은 수집 날짜·시각·신규 건수 셋뿐이다. "데이터 기준:" 라벨·"다음 갱신"·지연 문구는 두지 않는다. 신규 건수 = **직전 산출물에도 있던 매각기일에 한해** 직전 id 전량 어디에도 없던 이번 산출 물건 수(`meta.newCount` — 2026-08-05 정정). 배분 창이 하루 굴러 들어온 새 기일분은 신규가 아니다 — 옛 정의(단순 id 차집합)는 창 회전량을 신규로 세어 실측 431건 중 대부분이 회전분이었다. 비교 대상이 없거나 겹치는 기일이 0이면 0으로 단정하지 않고 감춘다(날짜·시각만 남긴다).
- 무가입 — 관심함·최근 본 물건은 localStorage `crazyvalue.*`. 코드 위치는 이 저장소(`C:\dev\crazyvalue`)만, 기획 문서는 기획 폴더.
- 워드마크: 미친가치(주)+CrazyValue(보조). "미친"은 브랜드명·픽 배지 한정, 본문 수식어 금지.

## 2. 도메인 불변식
- 저감률은 법원별 20% 또는 30% — 전국 일률 아님. 앱이 임의 계산·단정 표기 금지, 수집값 그대로.
- **미친가치 픽 = `priceRatio <= 0.5`** (최저가 ≤ 감정가 50%) — 저장 필드 아닌 파생 계산, 기준은 앱 안내에 공개.
- `priceRatio` = minPrice/appraisalPrice(0~1). 표시 할인율 = 1 − priceRatio — **부호 반전 주의**.
- 법적 고지(참고용·원문 우선·입찰 전 확인)를 상세·안내에 상시 표기. 투자 권유·수익 보장 표현 금지.
- 크롤 예절: 간격 ≥1초 · UA `CrazyValueBot/0.1` · 심야 실행 · 실패 시 직전 데이터 유지(무커밋).
- 기일 계산은 Asia/Seoul date-only, `src/lib/format.ts` 단일 함수만 경유. 금액은 원 단위 정수 저장, 축약은 표시 단계.

## 2-1. 작업 방식 (2026-07-18 주인님 지시 — 상시 적용)
- **Workflow 병렬 오케스트레이션 의무**: 실행 작업(감사·구현·수정·검증)은 서브에이전트를 최대 활용한 병렬 오케스트레이션(Workflow)으로 수행한다. 오케스트레이션 없이 단독 순차 작업으로 진행하지 않는다(경미한 단일 파일 수정·문서 갱신은 예외).
- **자율 마감**: 작업 단위 완료 시 "마감할까요" 류 재문의를 하지 않는다. 검증(테스트·게이트·E2E) 통과 → 커밋(머지)까지 스스로 마감하고 결과만 보고한다. 사용자 결정이 필요한 지점은 파괴적 변경·범위 변경·외부 퍼블리시 권한뿐.
- **방문자 관점 감사 프로토콜**: 기능 마감 후에는 방문자 Flow(최초 방문→온보딩(맞춤설정)→탐색→관심등록→재방문 맞춤화면→탭·백링크) 전수를 실브라우저로 감사하고, 발견 개선사항을 아래 §5 백로그에 순번으로 등록한 뒤 라운드 단위로 해소한다. 참고: 로그인·로그아웃·회원가입은 §11 범위 제외 확정(무가입 정체성) — 감사에서는 무가입 대체 흐름(온보딩 prefs·localStorage)을 대상으로 한다.

## 2-2. 방문자 감사 헌법 (2026-07-22 주인님 승인 — 라운드 불문 상시 적용)
> 1차 라운드는 트랙 설계를 매 회 새로 짜 커버리지가 라운드마다 흔들렸다. 범위를 매트릭스로 고정해 "빠진 곳"을 기계가 판정하게 한다.

1. **범위 = 35셀 매트릭스가 전부.** 여정 7단계(J1 최초 방문 · J2 관심조건 등록 · J3 맞춤설정(내 설정) · J4 탐색→법원 원문 · J5 관심 추적 · J6 재방문·주간 갱신 반영 · J7 이탈·탭/백링크) × 품질 5축(Q1 불안정성 · Q2 편의성 · Q3 정합성 · Q4 접근성·문구 · Q5 성능). **셀 공란 1개 = 감사 미완, 마감 불가.**
2. **증거 없는 발견은 등재 금지.** 실브라우저 재현 절차와 증거(스냅샷·측정치·콘솔 출력)를 동반한 건만 백로그에 올린다. 코드 읽고 추정한 건은 등재하지 않는다.
3. **적대적 교차검증 통과가 수정 착수 조건.** 등재 건은 독립 에이전트가 반증을 기본값으로 재현 시도한다. 재현 실패·확정 사양·기존 라운드 중복은 폐기하고 사유를 남긴다. 심각도 "불안정"은 3중 검증.
4. **순번 불변.** 1차 라운드(1~30)를 이어 31번부터 부여하고, 이후 라운드에서 재번호를 매기지 않는다.
5. **무가입 정체성 유지 — 인증 도입 금지.** 주인님 지시의 "로그인 등록/로그아웃"은 각각 **관심조건 등록 / 내 데이터 초기화**로 사상해 감사·구현한다.
6. **법원 원문 도달 불변식(2026-08-20 개정 — "존재"에서 "도달"로).** 사건 단위 딥링크는 법원 사이트 구조상 불가하다(`docs/CRAWLER.md` §4.1). 앱은 ① 원문 화면 링크 ② 번호 복사 ③ 찾는 방법 안내 **3종을 모두** 제공한다. **3종이 있는 것만으로는 통과가 아니다** — 개정 전 조문은 존재만 요구했고, 그래서 1,000건 전량이 오류창으로 떨어지는 동안 게이트 3종이 초록이었다(§9). 판정값은 목적지 계약이다: ⓐ 링크는 **단독 진입이 되는 최상단 화면**(`…F00`/`…M00`)이어야 한다 — `…F01`/`…M01`은 부모가 사건 파라미터를 넣어 줘야 뜨는 자식 화면이다 ⓑ 복사값은 목적지 **입력칸 규격**(숫자·7자 이내)을 산출물 전건에서 통과해야 한다 ⓒ 안내가 지목하는 법원 표기는 **선택 목록에 실제로 있는 이름**이어야 한다(`tests/unit/court-select-options.ts` 스냅샷 대조). 세 판정은 `tests/unit/court-origin-reach.test.ts`가 기계로 채점하고, **목적지가 실제로 열리는지는 외부 네트워크가 필요하므로 라이브 게이트로 따로 잰다** — 화면 ID를 규격에 다시 박지 않는다(박아서 결함이 고정된 것이 §9의 detail-link 게이트다).
7. **감사·수정·검증은 Workflow 병렬 오케스트레이션으로만 수행한다**(§2-1 의무의 감사 라운드 적용형). 변이 구간은 그룹별 파일 소유권을 배타 배정해 병렬화한다.

## 3. 마감 게이트
- 각 Phase = PLAN §8 DoD 기계 검증 + §13 규칙 15 점검 + TRACE.md 행 단위 대조(공란 0) 통과 후에만 커밋·마감.
- UI(.tsx) 변경 시: `node .claude/hooks/ui-quality-gate.mjs --check` → 위반 0건 → `--pass` (Stop 훅이 미검증 마감을 차단).
- **데이터 산출물(`public/data/**`·수집 파이프라인) 변경 시: `node .claude/hooks/data-value-gate.mjs --check` → 위반 0건 → `--pass`.** 채점 기준은 파이프라인 성공이 아니라 **방문자 가치** — R1 기일 커버리지 · R2 배분 창(`OUTPUT_WINDOW_DAYS` 7일) 생존 · R3 지역 0건 상한 · R5 신호 정합. **R4(갱신 체감)는 2026-08-05에 폐기했고 번호는 재사용하지 않는다**(순번 불변 — 사유는 §9 원장). 판정 기준 시각은 실행 시각이 아니라 `meta.crawledAt`이라 며칠 뒤 돌려도 결과가 같다. **"런 성공·배포 반영"만 확인하고 마감하지 않는다**(2026-08-02 질책의 근본 처방).
- 커밋 규약: `feat:`/`fix:`/`chore:`/`data:` + Phase당 최소 1커밋. force-push·`--no-verify` 금지. 신규 의존성 = 커밋 메시지에 도입 사유 1줄.
- main은 항상 배포 가능 상태. push = Vercel 자동 배포.
- **세로 리듬(2026-07-19 주인님 확정)**: 모바일 화면 세로폭 낭비 금지 — 섹션 간 16px 기본(`space-y-4`), 설명문 1줄 `leading-snug`, **안내·상태 문구 1문장 압축(2문장 병렬 금지)**, 레이어 간 하단 패딩 중복 금지. 상세 기준·예외(ErrorState 규격·법적 고지·읽기 본문)는 `design-system/MASTER.md` 세로 리듬 오버라이드(게이트 R11·R12 warn 연동).

## 9. 오류 원장 (Error Ledger)
- **[2026-08-16] 재방문 목록→상세 404 (SW 캐시 전략).** 증상: 재방문자가 목록에서 물건을 탭하면 상세가 404(실브라우저 재현 — RSC 프리페치 404 7건, 상세 이동 HTTP 404). 원인: `/data/*.json`이 StaleWhileRevalidate라 재방문 첫 화면이 직전 산출물로 그려지는데, 상세는 `dynamicParams=false` 정적 생성이라 매일 물갈이에서 빠진 id는 페이지가 없다 — 목록과 상세가 서로 다른 날짜의 데이터를 봤다. 처방: NetworkFirst + `networkTimeoutSeconds: 4`(온라인 = 항상 이번 산출물, 오프라인·저속망 = 직전 데이터 폴백, §1 계약 유지). 회귀 고정: `tests/unit/sw-cache-policy.test.ts`. meta.json이 2026-08-09에 같은 원인으로 같은 처방을 받았는데 지역 데이터에 일반화하지 않은 것이 재발 경로였다.
> 형식: `- [날짜] 증상 → 원인 → 처방 1줄`
- **[2026-08-20] 상세의 "법원경매정보에서 원문 보기"가 1,000건 전부 오류창으로 떨어진다(핵심목표 미달).** 증상: 새 탭에 WebSquare 오류창 "부모 객체를 찾을 수 없습니다." + 콘솔 `TypeError: Cannot read properties of null (reading 'scglblo')`, 화면은 법원·사건번호가 빈 사건상세 + "해당 사건번호는 잘못된 번호입니다."(신규 방문자·모바일 390px·쿠키 0 조건에서 재현). 원인: `DETAIL_URL`이 가리키는 `PGJ15AF01.xml`은 **부모 화면이 wframe 파라미터로 사건을 넣어 줘야 뜨는 자식 화면**이다 — 그 화면 `onpageload`가 `$p.getParameter("param")`으로 `csNo`·`cortOfcCd`를 받고 최상단 전역 `scglblo`를 참조하는데, 단독 진입은 부모가 없어 둘 다 실패한다(법원 사이트 규칙: `…F00`/`…M00` = 최상단 컨테이너, `…F01`/`…M01` = 자식). 2026-07-18 정찰이 `GET 200`만 보고 렌더를 보지 않은 것이 통로다(같은 날 원장의 "브라우저 팬이 courtauction SPA를 못 띄움" 항목과 같은 뿌리). 처방: 목적지를 **경매사건검색 `PGJ159M00.xml`**(단독 진입 정상 실측)으로 교체 + 도달 게이트 신설 — 승인 대기(PLAN A).
- **[2026-08-20] 원문 도달 게이트 2종이 깨진 링크를 초록으로 통과시켰다.** 유닛·E2E가 모두 `href === item.detailUrl`만 단언한다 → **자기 자신과의 대조는 도달을 재지 않는다**(§2-2 조문 6도 3종의 "존재"만 요구한다). 처방: 조문 6을 "도달"로 개정하고, 판정값을 목적지 렌더(오류 모달 0 · 콘솔 error 0 · 기대 제목)로 바꾼다.
- **[2026-08-20] 게이트가 외부 화면 ID를 규격으로 박아 결함을 고정했다.** `tests/unit/detail-link.test.ts`(감사 90)가 `w2xPath`를 `PGJ15AF01.xml`로 단언하고 **데이터 전건을 그 값에 맞추라고** 요구했다 — 즉 전건이 규격과 일치할수록 전건이 오류창으로 떨어졌고, 그 상태가 초록으로 유지됐다. 처방: 판정값을 특정 화면 ID가 아니라 **구조 규격**(단독 진입 가능 = `…F00`/`…M00`)으로 바꾸고, 실제 도달은 라이브 게이트(`scripts/verify-origin-link.ts`)가 잰다. **남의 화면 ID를 우리 규격에 박지 않는다** — 그쪽이 개편하면 같은 사고가 반복된다.
- **[2026-08-20] 법원명 190건이 선택 목록에 없는 표기였다(도달 차단).** 우리는 본원을 붙여 쓰고(`대전지방법원 천안지원`) 법원 선택 목록은 지원명만 쓰거나(`천안지원`) 본원을 한 낱말로 붙인다(`부산서부지원`) — 안내가 지목하는 항목이 목록에 없어 방문자가 60개를 훑어야 했다. 처방: `src/lib/court-origin.ts`의 정규화 표 + 실물 60종 스냅샷(`tests/unit/court-select-options.ts`) 전건 대조 게이트. 뮤테이션으로 증명했다(표를 비우면 10종이 빨강).
- **[2026-08-20] `COURT_BY_CODE` 법원명 오매핑 — 앱이 틀린 법원을 표시한다(별건·순번 118).** 실측 4/4 확정: `2021타경6228`(대구 중구 물건)이 부산지방법원으로, `2025타경30912`(대구 달서구)가 울산지방법원으로, `2025타경10570`(경주)·`2026타경10122`(익산)이 부산서부지원으로 적혀 있고, **소재지 관할 법원에서는 같은 사건번호가 전부 조회된다.** 원인: 법원명 원천이 손코딩 표(`COURT_BY_CODE[boCd].name`)이고 응답의 `jiwonNm`은 폴백으로만 쓰인다 — region은 주소에서 뽑으므로(`resolveRegionKey`) 법원만 어긋난다. 처방(미착수): 원천을 응답 필드로 옮기고 표는 폴백으로 내린 뒤, 표본 조회로 검증한다. 상한 규모 = 법원 홈지역과 배치지역이 어긋난 183건(단 금산군→대전지법 같은 관할 예외와 자동차·건설기계의 사용본거지 예외는 정상이다 — 실측에서 그 3건은 도달했다).
- **[2026-08-20] `scripts/**`도 `next build`의 타입 검사를 받는다 — dotAll 플래그가 빌드를 깼다.** 새 검증 스크립트에 정규식 `/…/s`를 썼더니 `next build`가 `This regular expression flag is only available when targeting 'es2018' or later`로 죽었고, **E2E는 테스트가 아니라 webServer 기동 단계에서** 끝났다(`Process from config.webServer was not able to start`). 유닛·tsx 실행은 통과했다 — 스크립트를 tsx로 돌려 본 것은 빌드 통과의 증거가 아니다. 처방: 스크립트도 앱과 같은 target 제약을 받는다고 보고, 새 파일을 만든 뒤에는 **`npm run build`를 한 번 통과시킨다**(그리고 위 항목대로 빌드와 tsc를 겹치지 않는다).
- **[2026-08-20] 빌드와 타입검사를 겹치면 오탐이 난다.** 백그라운드 `playwright test`(=`npm run build`)가 도는 동안 `tsc --noEmit`이 `.next/types/**` 결측으로 TS6053을 뿜었다 — 코드 결함이 아니라 산출물이 재생성되는 중이었다. 둘을 겹치지 않게 돌린다.
- **[2026-08-20] 사건번호 복사값이 목적지 입력칸에 들어가지 않는다.** 법원 사이트의 사건번호 입력은 `연도(select) + 번호(input, maxlength=7, 한글 제거)` 2칸이라, 앱이 복사해 주는 `2023타경104819`를 붙여넣으면 필드에 남는 값이 **`20231`** 이고 검색이 실패한다(실측). 안내 문구 "사건번호를 붙여넣는다"도 같이 거짓이 된다 → 복사값은 **번호만**, 안내는 **법원·연도·번호 3칸 순서**로 맞춘다.
- [2026-08-17] `tests/e2e/prefs-home-sync.spec.ts:163`("탭 닫고 재방문")이 **플래키**다 — 3회전 중 1회 실패(2 워커 병렬에서만, 단독 실행·재실행은 통과). 새 컨텍스트 재방문이라 저장·서빙 타이밍에 걸리는 것으로 보이나 원인 미확정이다. **간헐 실패하는 테스트는 게이트가 아니다** — 다음 라운드에서 대기 조건을 명시적 단언으로 바꾼다. 그때까지 이 스펙의 초록은 1회전으로 판정하지 않는다.
- [2026-08-17] `npm run lint`가 **항상 실패**했다 → `eslint.config.mjs` ignores에 serwist 빌드 생성물(`public/sw.js`)이 없어 압축 코드가 `no-this-alias` error를 냈다(경고 89건 중 87건도 같은 출처). CI는 lint를 돌리지 않아 아무도 몰랐다 → 린트는 우리가 쓴 코드만 본다. **CI가 안 돌리는 명령은 조용히 썩는다.**
- [2026-08-17] **병렬 그룹 수정이 서로를 되돌렸다.** 증상: S3 5그룹이 각자 검증에서 전부 초록이었는데 통합 E2E에서 2건 실패. 원인: G1이 `ExitGuard` 센티넬 pushState에 Next `__NA` 마커를 보존했는데, G3가 홈 `commit()`을 모든 복원 경로에서 호출하게 만들며 `replaceState(null,…)`이 그 마커를 도로 지웠다 — 각 그룹은 자기 파일만 보고 격리 빌드에서 검증했다. 처방: **파일 배타 소유는 충돌을 막을 뿐 간섭을 막지 못한다** — 병렬 수정 뒤에는 반드시 통합 재빌드 1회전을 게이트로 둔다. 그룹 보고의 초록은 통합 초록이 아니다.
- [2026-08-17] **홈과 리스트의 history state 규약이 서로 반대다.** 홈(`app/page.tsx`)은 `window.location.search`를 직접 읽으므로 `replaceState`에 **state를 보존**해야 한다(null로 밀면 그 엔트리 뒤로가기가 문서 재로드가 된다). 리스트(`app/list/page.tsx`)는 `useSearchParams`가 원천이라 **state를 새로 써야** Next가 새 URL을 인식한다(보존하면 더보기가 죽는다 — 실측으로 확인). 한쪽 규약을 다른 쪽에 복사하지 마라.
- [2026-08-17] 원격에 없는 브랜치를 지울 뻔했다. 원인: 조사 에이전트가 **로컬 remote-tracking 참조**(`git branch -r`)를 원격 실물로 보고했다 — 두 브랜치는 이미 원격에서 삭제됐고 낡은 캐시만 남아 있었다. 처방: 원격 상태 판정은 `git ls-remote`로 하고, 정리는 삭제가 아니라 `git fetch --prune`이다.
- [2026-08-17] 소스 줄 수를 과소 계상해 기수·견적 근거가 흔들림(PrefsEditor 332 vs 실제 364) → PowerShell `Measure-Object -Line`이 **빈 줄을 세지 않는다** → 줄 수는 `(Get-Content -LiteralPath $p).Count`로 센다. 파일 경로에 `[id]` 같은 대괄호가 있으면 `-LiteralPath`가 필수다(와일드카드 문자 클래스로 해석돼 "파일 없음"이 된다).
- [2026-08-17] E2E 소요·건수를 추정으로 계획에 적을 뻔함 → `playwright.config.ts`의 webServer가 `npm run build && npm run start`라 **첫 실행에 빌드가 포함**되고, 서버가 살아 있으면 재사용해 시간이 완전히 달라진다 → E2E 소요는 서버 상태를 먼저 확인한 뒤 실측한다(실측 38건·2.4분).
- [2026-08-17] 소스 줄 수를 과소 계상해 기수·견적 근거가 흔들림(PrefsEditor 332 vs 실제 364) → PowerShell `Measure-Object -Line`이 **빈 줄을 세지 않는다** → 줄 수는 `(Get-Content -LiteralPath $p).Count`로 센다. 파일 경로에 `[id]` 같은 대괄호가 있으면 `-LiteralPath`가 필수다(와일드카드 문자 클래스로 해석돼 "파일 없음"이 된다).
- [2026-08-17] E2E 소요·건수를 추정으로 계획에 적을 뻔함 → `playwright.config.ts`의 webServer가 `npm run build && npm run start`라 **첫 실행에 빌드가 포함**되고, 서버가 살아 있으면 재사용해 시간이 완전히 달라진다 → E2E 소요는 서버 상태를 먼저 확인한 뒤 실측한다(실측 22건·3.3분).
- [2026-07-17] 비율 필드명 `discountRate`가 표시 개념과 반전 → `priceRatio`로 개명 → 저장 필드는 저장값 의미로 명명.
- [2026-07-17] Lighthouse 12부터 PWA 카테고리 삭제 → PWA 검증은 manifest 200 + SW 등록(Playwright)으로 → DoD 도구는 현행 버전 검증 가능성 확인 후 채택.
- [2026-07-18] create-next-app 기본 브랜치가 master로 생성 → 규칙(§13-8)은 main 전제 → 스캐폴드 직후 `git branch -m master main`으로 정합화.
- [2026-07-18] 게이트 템플릿이 루트 `app/` 구조·diff 기반 스캔이라 src/ 구조에서 미검출 + 커밋 후 --pass 시 빈 스캔 구멍 → src/(app|components) 전수 스캔으로 변경 → 채점기는 "변경분"이 아니라 산출물 전수를 기준으로.
- [2026-07-18] `gh repo create`·`vercel --prod`가 세션 자동 권한 분류기에 차단(외부 공개 액션) → 에이전트 단독 퍼블리시 불가 환경 → 주인님 1회 승인(권한 규칙 추가) 또는 직접 실행으로 해소, 로컬 Phase는 계속 진행하고 배포 검증은 해소 후 일괄 수행.
- [2026-07-18] watchlist 손상 JSON 테스트 실패 → safeRead가 공유 상수(EMPTY_WATCH)를 그대로 반환해 호출부 변이가 상수를 오염 → 기본값은 팩토리로 호출마다 새 객체 생성(공유 가변 기본값 금지).
- [2026-07-18] 내장 브라우저 팬에서 courtauction.go.kr(WebSquare SPA) 본문 렌더 실패(스크린샷 타임아웃·body 0바이트 지속) → 정부 SPA는 팬 환경과 비호환 가능 → 화면 XML 정적 파일 GET으로 enum·필드 기본값을 추출하는 우회를 기본 전략으로.
- [2026-07-18] 게이트 R1이 ErrorState 위임 페이지 3곳을 오탐 → 파일 단위 grep이 컴포넌트 경계를 미인식 → 위임 시 공용 컴포넌트의 role="alert" 보유를 검증하는 판정으로 게이트를 수정(제품 코드 무변경·엄격성 유지 — ErrorState에서 alert 제거 시 다시 차단됨).
- [2026-07-19] WatchCard 배지 E2E에서 getByText 부분 일치가 가시 배지+오버레이 sr-only 요약에 이중 매치(strict 위반) → 같은 문구가 배지·접근 이름에 중복 존재하는 구조 → 배지는 exact 전체 문자열, 접근 계약은 getByRole(link, name 정규식)으로 분리 단언(오버레이 카드형 UI 공통 처방).
- [2026-07-19] 내장 브라우저 팬이 프로덕션 URL 로드 직후 resize·screenshot 30s 타임아웃 반복(렌더러 불응, 2026-07-18 courtauction 선례와 동류) → 팬 재현성 한계 → 실측·스크린샷·측정 자동화는 Playwright MCP를 기본 채널로(E2E와 동일 엔진).
- [2026-07-19] 워크트리 검증에서 turbopack이 node_modules junction을 TurbopackInternalError("filesystem root 밖 심링크")로 거부 + 배경 체인이 `| tail` 뒤 exit 0으로 실패 은폐 → junction 의존성 공유 불가·pipefail 미설정이 원인 → 워크트리 검증은 `npm ci` 실설치, 배경 체인은 `set -o pipefail`, junction 제거는 rm -rf 금지(원본 삭제 위험 — `cmd rmdir`로 링크만 제거).
- [2026-07-19] 작업 트리 E2E 1건 실패(홈 결과 버튼 클릭이 배너 닫기에 가로채임) → 원인은 세로 리듬 변경이 아니라 **미커밋 InstallPrompt 배너(z-30 하단 고정)가 ResultButton(z-30 bottom-14)과 겹침** — HEAD+변경분 워크트리(npm ci) E2E 8/8 통과로 어트리뷰션 증명 → 타 트랙 미커밋분이 섞인 작업 트리에서는 커밋 트리 기준 워크트리 검증으로 분리 판정, 하단 고정 요소 신설 시 기존 고정 CTA와 겹침·z-순서 검사 의무(InstallPrompt 수정은 별도 작업 칩 등록).
- [2026-07-19] 브라우저 실측에서 하이드레이션 직후 클릭+측정을 한 호출에 묶자 언마운트 레이스로 엉뚱한 요소(온보딩 시트 잔상 p)를 측정 → 상태 변경(클릭)과 측정은 별도 호출로 분리하고, 측정 대상은 전역 텍스트 검색이 아니라 컨테이너 스코프 셀렉터(`a[href*="pick=1"] p`)로 지정한다.
- [2026-07-22] "동시 8기수" 계획이 실현 불가 — Workflow 동시 상한은 `min(16, CPU코어−2)`이고 이 장비는 4코어라 **2**다 → 기수 계획을 코어 수로 먼저 나눠 파도 수를 산정하고, 벽시계 추정에 반영한다(총 작업량이 아니라 파도 수가 시간을 결정한다).
- [2026-07-22] 7트랙 병렬 감사에 MCP 브라우저를 쓰려다 세션이 1개뿐이라 서로의 페이지를 덮어쓸 구조였음 → 에이전트별로 **자기 소유 Playwright 스펙 파일을 작성·실행**하는 방식으로 전환 → 병렬 충돌 0 + 헌법 §2-2 조문 2가 요구하는 재현 증거를 테스트 출력으로 확보. 서비스워커는 dev에서 비활성이므로 PWA 감사는 반드시 프로덕션 빌드에서 한다.
- [2026-07-22] 백로그 상태열을 정규식 일괄 치환하다 문서 1행(제목)이 훼손됨 → `.replace`의 앵커가 의도한 표 행 밖에서도 성립할 수 있는데 치환 후 검증을 하지 않았다 → **문서 일괄 치환은 행 단위로 찾아 끝문자열을 검사한 뒤 교체하고, 치환 직후 head/grep으로 훼손 여부를 확인한다.**
- [2026-07-22] 93번(배너·고정 CTA 겹침) 수정 후에도 E2E 1건 실패 — 배너가 **일반 콘텐츠**(리스트 더보기 버튼)를 덮었다. 수정이 `position:fixed` 요소만 회피했기 때문 → fixed 요소는 레이아웃을 밀지 않으므로 **실측 높이를 CSS 변수로 알리고 `main` 하단 여백에 더한다**(`--cv-banner-h`). 하단 고정 요소 신설 시 고정 CTA 겹침과 스크롤 끝 콘텐츠 가림을 **둘 다** 검사한다.
- [2026-07-22] 기획 문서(CRAWLER.md §2.6)가 수집 규모를 "수십 요청 이내"로 적었으나 실측은 **16,302건·약 5.2시간**으로 2~3자릿수 어긋났고, 그 추정 위에 워크플로 상한을 설계했다 → **외부 API 규모는 기획 추정이 아니라 착수 시 1회 실측(dry-run)으로 확정**하고 문서의 추정치를 실측치로 교체한다.
- [2026-07-22] 수집 시간 단축책으로 "매각기일 창 축소"를 채택할 뻔했으나 실측은 42일 16,302건 → 21일 **17,000건**으로 무효였다(매각기일이 근시일 3주에 집중) → 대조군 실험(0일 창 934건)으로 **필터가 작동하는가**와 **그 조작이 효과가 있는가**를 분리 확인한 뒤 채택한다. 그럴듯한 인과(창을 줄이면 건수가 준다)를 측정 없이 믿지 않는다.
- [2026-07-22] 홈 세로폭 절감 중 탐색 에이전트가 홈 컨테이너 `pb-8`을 `main`의 `pb-[calc(6rem+…)]`과 "하단 패딩 중복"이라 지목했으나 제거하지 않음 → ResultButton이 `position:fixed bottom-14`(바닥 ~120px 점유)라 `pb-8`은 그 고정 CTA 클리어런스를 채우는 **기능성 여백**(제거 시 스크롤 끝 카드가 가려짐, 선례 61번과 동류) → "중복처럼 보이는 하단 여백"은 fixed 고정요소 클리어런스인지 실측(고정요소 top Y + 8px) 후에만 제거한다.
- [2026-07-22] (사양 이탈 기록) 홈 첫 화면 기준을 "유찰 2회 이상으로 값이 내려간 물건" **단일 기준으로 통일**(주인님 지시) — 데이터셋 전체가 이미 유찰 2회+라 "미친가치 픽(감정가 50%)" 배너가 경쟁 기준이 되어 첫인상 혼란(제10조 중복/병립) → 홈의 `PickEntry` 제거(`page.tsx`), 감정가 50% 기준은 리스트 카드 픽 배지·`/list?pick`·`/guide`에만 부가정보로 존치 → **PLAN.md §4.3-①·TRACE.md(홈에 픽 진입 포함) 동기화 필요**(다음 편집 시 이 결정으로 갱신, 픽 진입 복원 금지). `src/components/PickEntry.tsx`는 현재 import 0의 고아 파일(빌드 무해) — 후속 정리 대상.
- [2026-07-22] 세로폭 수정 마감 검증 중 프로덕션 `/`에서 React #418(하이드레이션 불일치)이 간헐 관측 → **로컬 급속 재빌드 중 이전 빌드의 서비스워커(serwist)가 옛 청크를 캐시로 물고 새 프리렌더 HTML과 섞인 테스트 아티팩트**였다(dev엔 SW 없어 0·prod만 발생, 같은 화면의 prod 프리렌더 HTML == dev 요청시 HTML 바이트 동일 → 실제 렌더 불일치원 없음). `navigator.serviceWorker` 등록해제 + `caches` 전삭제 + localStorage clear 후 프레시 리로드 시 0. → **PWA(SW) 앱을 로컬에서 여러 번 재빌드하며 같은 포트·같은 브라우저로 검증할 때 하이드레이션/RSC 404 에러가 나오면 코드부터 의심하지 말고 SW·caches부터 비우고 재확인**한다. 실배포는 배포별 일관 + serwist precache 버저닝으로 무해.
- [2026-07-27] 주간 갱신이 한 번도 성사되지 않음(프로덕션은 여전히 목데이터) → 원인 3중: ① 스케줄 기회가 2회뿐이었고 07-19분은 리포지토리 생성(07-18 20:01Z)이 cron 시각(07-18 18:00Z)보다 늦어 미등록, ② 07-26분은 실행됐으나 43분·약 2,160요청 지점에서 `ipcheck=false` 로봇탐지 — 계획 총량 9,307요청(2.9시간)의 25% 지점이라 **현 설계로는 완주 자체가 불가능**, ③ 어떤 실패든 쓰기 0·exit 1인 all-or-nothing 계약이라 확보한 18,884건이 전량 폐기 → 처방: 상세 요청을 차단 임계(실측 약 2,300회) 아래로 감축 + 차단을 조기종료로 강등해 부분 산출·커밋 + 요일 분할 누적. **"실패 시 직전 데이터 유지"는 직전 데이터가 실데이터일 때만 안전망이다 — 초기 목데이터 상태에서는 영구 정지 장치가 된다.**
- [2026-07-27] 위 실패의 요청 수를 로그가 아니라 경과시간÷1.2초로 역산해야 했음 → `liveRequestCount`가 dry-run 분기에서만 출력됨(crawl.ts:776) → **요청 카운터·차단 시점은 실행 모드와 무관하게 항상 로깅**한다(차단 임계는 사후 재현이 불가능한 1회성 관측치다).
- [2026-07-27] `BUDGET.maxRequests`가 착수 전 1회 계산치라 상한이 지켜지지 않음 → `request()`는 재시도마다 카운터를 올리는데 목록·상세 루프가 예산을 재대조하지 않았다(상세 1,026건 × 재시도 4회 = 상한의 3배) → **상한은 계획치가 아니라 루프 선두의 러닝 체크로 강제**한다(목록 소진=전체 실패, 상세 소진=조기 종료).
- [2026-07-27] HTTP 429를 5xx와 같이 재시도 대상으로 분류 → 서버가 감속을 요구한 순간 물건당 요청이 4배로 늘어 차단을 가속 → **레이트리밋은 Retry-After를 존중해 길게 1회만 쉬고 반복되면 중단 신호로 승격**한다(CRAWLER.md §4 "429/550 시 지수백오프·중단").
- [2026-07-27] 최저가 결측 시 목록가(`minmaePrice`, 실측상 직전 회차 가격) 폴백이 `minPrice==감정가`를 만들어 priceRatio 1.0·저감률 0 이력이 zod를 통과 → superRefine은 저장값끼리의 정합만 보므로 원리적으로 검출 불가 → **도메인 불가능값은 스키마가 아니라 매핑 단계 스킵 규칙으로 막는다**(유찰 2회 이상인데 최저가≥감정가면 제외).
- [2026-07-27] `calibrateFailedCode`가 관측 1회짜리 코드로 유찰 결과코드를 확정 → `count > bestCount` 비교에 표본 하한·마진이 없어 조기 종료로 표본이 얇아지면 유찰/변경 라벨이 뒤집힌 채 게이트 통과 → **런타임 자가 보정에는 표본 하한(200건)과 2위 대비 우세 배수(3배)를 함께 강제**하고 채택 근거를 로깅한다.
- [2026-07-27] `partial` 한 필드에 "차단됐다"와 "후보가 예산보다 많다"를 겹쳐 담아 상시 참이 됨(신호 소실) + 조기 종료가 exit 0이라 `if: failure()` 알림이 발화하지 않아 전량 역산 이력이 조용히 배포될 경로 → **운영 신호는 의미 단위로 분리**(aborted·abortReason·abortedAtRequest·totalRequests·truncated·historySource·scope)하고 워크플로가 meta로 조기 종료를 판정해 이슈를 만든다.
- [2026-07-27] `needsDetail`(픽 후보 판정)이 `minmaePrice`를 쓰는데 실제 `minPrice`는 `notifyMinmaePrice1`에서 온다 → 판정 가격과 산출 가격이 **다른 필드**라 비율이 한 회차분 과대평가되고 실제 픽이 상세 후보에서 탈락(dry-run 표본 5건 전부 후보 0 — 실제 0.80을 1.00으로 판정) → **같은 값을 판정과 산출에서 두 번 유도하지 말고 폴백 체인을 공유**한다(상세 의존 항만 제외).
- [2026-07-27] `report failure`의 `if: failure() && steps.gate.outputs.run == 'true'`는 게이트 스텝 자신이 실패하면 outputs가 빈 문자열이라 거짓이 돼 **유일한 실패 알림이 침묵** → 조건을 `!= 'false'`로 뒤집어 미판정도 알림 대상에 넣는다. **가드 조건은 fail-safe 방향(모르면 알린다)으로 쓴다** — 알림 경로에서 fail-open은 침묵 실패다.
- [2026-07-27] 커밋 제목에 `@` 문자가 새어 들어가 규약(`fix:` 접두)이 깨짐(bf2a778) → Bash 도구에 PowerShell here-string(`@'…'@`)을 써서 리터럴 `@`가 메시지 첫/끝 줄로 들어갔다 → **셸이 다르면 다중행 문자열 문법도 다르다** — Bash는 heredoc, PowerShell은 here-string. 본문은 온전하고 force-push 금지(§3)라 이력은 정정하지 않는다.
- [2026-07-27] **(주인님 지시 2회 = 하네스 실패)** "자동 설정"을 지시받고도 게이트에 날짜 `2026-07-28`을 하드코딩하고 "드롭되면 수동 실행하세요"를 운영 절차로 남겼다 → 1회성 조건은 그 한 번을 놓치면 영구 미복구다(GitHub은 고부하 시 스케줄을 지연이 아니라 **드롭**한다 — 런도 이슈도 흔적이 없다) → **자동화의 판정 기준은 "날짜·횟수"가 아니라 "목표 상태"로 쓴다.** 캐치업 슬롯 `0 18 * * 0-5`가 데이터 나이 ≥7일일 때만 매일 03:00 KST에 스스로 재시도한다(성공하면 나이 0이라 조용해진다). **복구 경로에 사람이 남아 있으면 자동화는 미완이다** — 마감 전 "이 설계에서 사람이 손대야 하는 지점이 하나라도 있는가"를 자문한다.
- [2026-07-27] (기각 기록) 검토자가 `notifyMinmaePrice1`을 미검증 컬럼으로 단정했으나 dry-run 실측이 반증 — 상세 0건 실행에서 최저가가 감정가의 80%로 산출된 것은 이 컬럼이 채워졌다는 증거다 → **폴백 체인의 어느 항이 발화했는지는 산출 수치에서 역추적할 수 있다**(코드만 읽고 "미검증"으로 단정하지 않는다).
- [2026-07-30] **(주인님 질책 = 하네스 실패)** 파이프라인 수정의 검증을 야간 슬롯(하루 1회)에 위탁해 5일간 실패 모드를 하루 1개씩만 소진(07-25 차단 → 07-28 중복 기각 → 07-29 fetch failed) → **파이프라인 수정은 당일 `workflow_dispatch` 1회로 실전 등가 검증까지 마감**한다. 야간 슬롯은 검증 수단이 아니라 정상 운영이다.
- [2026-07-30] 게이트 철학 전환 — "오류 0건이어야 배포"가 예상 가능한 환경 이상(서버 재서빙 중복)을 전량 실패로 증폭(07-28 고유 약 9,400건 확보하고도 폐기) → **게이트는 유효 부분집합 상시 배포로 설계하고 이상은 개별 드롭+카운트 로깅, 기각은 유효 0건일 때만.** 07-27 "부분 산출·커밋" 처방을 게이트 자신에게까지 적용한 완결편.
- [2026-07-30] 재서빙 중복은 야간 배치 창 전유물이 아니다 — 주간 dry-run 첫 페이지에서도 40행 중 16행 중복 실측 → **수집량 계산·페이지네이션 종결 판정은 항상 고유 건수 기준**으로(중복 포함 누적치는 언제나 오염돼 있다).
- [2026-07-30] 03:40 KST에 당일 캐치업 미발화를 "드롭 재발"로 단정했으나 선례상 발화는 53~83분 지연(18:53~19:23Z)이 정상 범위 → **드롭/지연 판별은 슬롯 시각+2시간 이후에만 확정**한다(그 전 판정은 추측이다).
- [2026-07-30] 대기열 런의 checkout은 시작 시각이 아니라 **이벤트 생성 시점의 SHA로 고정** — concurrency 대기 후 시작한 캐치업이 직전 런의 데이터 커밋(25분 전 이벤트 이후 푸시)을 못 보고 옛 meta로 run=true 이중 실행, 오탐 실패 이슈 #5 생성 → 게이트 판정 직전 `git pull --ff-only`로 최신 main 정렬. 대기열을 거친 런의 워킹트리는 항상 낡았다고 가정한다.
- [2026-07-30] 로봇탐지 임계 실측 2점 — 약 2,160회(07-26)·1,123회(07-30): 임계는 상수가 아니라 변동한다 → 방어는 예산 하향(회피 보장 불가)이 아니라 **강등**(조기 종료+부분 배포)이 정답. 세션 GET transport 거부는 로봇탐지 중단 1분 뒤 재현(누적 표본 2) — 상관은 가설로만 기록, 3회째 관측 시 실행 시각 분산 검토.
- [2026-07-23] 휴대폰 홈에서 하단 고정 CTA·탭바가 오른쪽으로 밀려 잘림(주인님 "프레임이 개판") → 원인은 **ItemCard 내부 `sr-only`(position:absolute)에 기준 블록이 없어 가로 캐러셀(RecentViewed·NewThisWeek의 `overflow-x-auto`) 밖으로 탈출**, 문서 `scrollWidth`를 631(뷰포트 397)로 늘려 모바일 레이아웃 뷰포트가 확장된 것(고정요소는 넓어진 레이아웃 뷰포트 기준으로 배치돼 밀림) → **ItemCard 루트에 `relative` 추가**로 기준 블록 복원(scrollWidth 412 = 뷰포트, 고정요소 l0/r412). **규칙: 가로 스크롤 컨테이너 안에 절대배치 자손(`sr-only` 포함)을 두면 반드시 조상에 `relative`를 준다.** 또 이 증상은 **"최근 본 물건"이 있는 상태에서만** 재현돼, 빈 상태만 캡쳐한 앞선 감사가 놓쳤다 → 회귀 가드 `tests/e2e/layout-overflow.spec.ts` 추가. **UI 감사는 빈 상태만 보지 말고 데이터가 채워진 상태를 반드시 포함한다.**
- [2026-08-02] 08-01 정기 슬롯은 성공·배포됐으나 산출 1,000건 **전량이 매각기일 08-03 하루**(실측: distinctDates 1) → `OUTPUT_CAP`(1,000)이 월요일 하루 물량보다 작아, 유효 9,359건을 임박순으로 자르면 첫날에서 소진된다(직전 주는 목요일 캐치업이라 07-30·07-31·08-03 3일로 우연히 분산됐을 뿐 같은 결함) → 갱신 주기 7일 대비 산출물 유효수명 1일: **08-04(화)부터 전 물건이 "기일 경과"**(앱은 경과분을 후순위 정렬만 하고 숨기지 않는다 — 백로그 12) + 그날 기일이 없는 서울·대구·광주·울산·제주가 통째로 0건. **상한은 "건수"가 아니라 "다음 갱신일까지의 기일 커버리지"로 잡는다**(예: 기일별 균등 배분 또는 창 전체 표본). 조기 종료(이슈 #6)는 이력 정밀도 문제일 뿐 이 결함의 원인이 아니다 — 상한만으로 재현된다.
- [2026-08-02] **(주인님 질책 = 하네스 실패)** 갱신 점검을 "런 성공 → 배포 반영 → 정상"까지만 확인하고 마감하려 했다 → **검증이 파이프라인 안쪽에서 멈추고 방문자 화면까지 오지 않았다** — 산출 1,000건이 하루로 수렴하고 시도 5곳이 0건이고 "이번 주 신규"가 0건인데도 게이트·런·배포가 전부 성공이었다(성공 신호가 실패를 덮는다) → **점검 순서를 뒤집는다: 방문자 화면(홈 첫 진입 상태)을 먼저 보고 그 다음에 파이프라인을 본다.** 기계 처방은 `data-value-gate.mjs`(§3) — 파이프라인 성공이 아니라 방문자 가치를 채점하고 미달이면 마감을 차단한다. **"성공했다"는 신호를 늘리는 대신 "쓸모없다"를 잡는 채점기를 늘린다.**
- [2026-08-02] 갱신이 됐는데 홈에 그 사실이 드러나지 않았다(주인님: "홈화면에서도 표현이 되어야 방문자가 인식한다") → 표기가 `데이터 기준: …`이라는 수동형 회색 12px 한 줄뿐이었고, 갱신을 체감시킬 "이번 주 신규" 섹션은 0건이라 사라졌다 → `isFreshUpdate`(수집일 3일 이내)로 **"새로 갱신" 배지**를 기준일 앞에 붙이고(값 중복 없이 수식만), 신규 0건은 게이트 R4로 차단한다. **갱신은 "일어났는가"가 아니라 "화면에서 보이는가"로 판정한다.**
- [2026-08-02] quick 모드 첫 실전 런이 세션 취득 단계 `TypeError: fetch failed` 재시도 3회 소진으로 죽었다(라이브 요청 4회·107초) → 원인은 코드가 아니다(로그상 `MODE=quick` → `--window 7 --no-detail` → 창 7일·상세 생략이 정확히 적용됐다). **하루에 크롤을 4회(정기 52분 + 재수집 60분 + 로컬 경량 + quick) 돌린 뒤의 접속 거부**로, 원장 2026-07-30 "세션 GET transport 거부"의 **3회째 관측**이다 → 쿨다운 6시간 설계의 실증이다. 처방: ① 리프레쉬 쿨다운 유지 ② 화면이 직전 런 실패를 말하게 한다(`lastConclusion` — 폴링이 running→idle로 조용히 돌아가면 방문자에게는 "눌렀는데 아무 일도 없었다"로 남는다) ③ 3회째 관측이므로 **실행 시각 분산**을 다음 후보로 둔다.
- [2026-08-02] E2E가 `C:\dev\zerospipe.co.kr`의 vitest 파일을 주워 "No tests found"로 죽었다 → 셸의 작업 디렉터리가 상위 `C:\dev`로 올라가 있었고(도구별 cwd는 서로 독립이며 턴 사이에 유지된다) 러너가 형제 프로젝트까지 스캔했다 → **테스트·빌드 명령은 저장소 경로를 명시해 실행한다.** 낯선 파일 경로가 오류에 보이면 코드가 아니라 실행 위치를 먼저 의심한다.
- [2026-08-02] 리프레쉬 E2E 5건이 전부 실패해 코드를 의심했으나 실제 화면은 **404**였다 → `playwright.config`의 webServer가 `npm run start`(빌드 없음) + `reuseExistingServer: true`라 **새 라우트가 없는 옛 `.next`를 서빙**했다(탭 목록에도 리프레쉬가 없었다 = 옛 번들의 증거) → webServer command를 `npm run build && npm run start`로 바꾸고 CI에도 빌드를 명시한다. **E2E가 통째로 실패하면 코드보다 먼저 "서빙되는 산출물이 최신인가"를 본다** — 스냅샷의 404·옛 UI가 그 신호다.
- [2026-08-02] (실측 기록) 리프레쉬 경량 모드 = 창 7일 + 상세 생략 → **182초·요청 162회**로 전량 모드(3,617초·1,030회)의 **5%**인데 산출 품질은 동일했다(1,000건·기일 5일·지역 17개·0건 시도 0개) + **처음으로 조기 종료 없이 완주**(요청 수가 차단 임계 1,030~2,160을 크게 밑돈다) → 병목은 목록이 아니라 **상세 요청**이었다(목록 1.1초/회 대 전체 평균 3.5초/회). 창 규모도 실측으로 확정: 3일 4,847건 · 7일 6,431건 · 10일 11,674건 · 42일 16,302건 — **원장 2026-07-22의 "창 축소 무효"는 21~42일 구간 한정이고 10일 미만에서는 급격히 준다.** 주간 전량 모드의 상세 비중은 재검토 여지가 있으나 주인님 지시(주간 설정 유지)에 따라 건드리지 않는다.
- [2026-08-02] E2E 3개 파일이 실데이터에서 깨진 채 방치돼 있었다(smoke: `seoul.json[0]` 로드 실패 · layout-overflow: 고정 id 3개라 "최근 본 물건" 섹션 미출현 → 회귀 가드 무력화 · empty-result: "토지×~5천만=0건" 전제가 실제 268건) → 전부 **목데이터 시절 고정 픽스처**이고, 푸시 시 자동 실행 경로가 없어 실데이터 전환 후 한 번도 돌지 않았다 → ① 픽스처를 산출물에서 결정적으로 선택(`tests/e2e/fixture.ts` `pickRegion` · 0건 조합 동적 탐색) ② `ci.yml` 신설로 푸시마다 typecheck·unit·E2E 자동 실행. **데이터 의존 테스트는 "어느 지역·어느 조합인가"를 고정하지 말고 "조건을 만족하는 것"을 매번 고른다. 로컬에서만 도는 검증은 결국 돌지 않는다.**
- [2026-08-02] git 로그 시각을 `--date=format-local:'… UTC'`로 뽑아 KST 값에 UTC 라벨을 붙일 뻔함(cron 슬롯 판정이 9시간 어긋난다) → `format-local`은 로컬(KST) 포맷이다 → **시각 라벨은 포맷 문자열에 손으로 쓰지 말고 `--date=iso-strict`(오프셋 동반)로 출력**한다.
- [2026-08-05] Git Bash(Windows)에서 `TZ=Asia/Seoul date`가 tzdata 부재로 `+0000`을 반환 — UTC 값을 KST로 읽어 "08-04 캐치업 슬롯 드롭"으로 오판할 뻔했다(실제로는 슬롯 32분 **전**이었다) → **로컬(Windows·Git Bash)에서 KST는 셸 TZ에 맡기지 말고 `node -e "new Date().toLocaleString('sv',{timeZone:'Asia/Seoul'})"`로 구한다.** 원장 2026-08-02(시각 라벨)와 같은 뿌리 — 이 저장소의 판정은 전부 KST 기준이라 시각 획득 경로부터 고정한다. **[2026-08-05 정렬] 이 조문의 적용 범위는 로컬 셸이다** — GitHub 러너(ubuntu)에는 tzdata가 있어 `TZ=Asia/Seoul date`가 정상 동작한다(실측: 런 30943387521은 19:27:54Z에 `KST 2026-08-05`를 출력했다). 그래서 `crawl.yml`은 러너에서 `TZ=Asia/Seoul date +%F`를 쓴다. 조문을 환경 무관 금지로 읽어 코드와 어긋난 것이라 **코드를 정답으로 두고 조문에 범위를 명시**했다. 관측을 규칙으로 승격할 때는 관측한 환경을 함께 적는다.
- [2026-08-05] **R4(이번 주 신규) 위반은 quick 모드의 수학적 귀결이다** — `--no-detail`은 이력을 전량 역산하고 역산은 회차 간격을 **35일 고정**으로 깐다(실측: 산출의 유찰 2회 물건 73건이 전부 2회째 유찰일 2026-07-03 = 기일 08-07 − 35). R4 판정창은 최근 7일이므로 역산 이력은 **절대** 들어올 수 없다 → 이슈 #9·#11·#12·#16이 같은 사유로 반복 생성됐다. **역산 이력 위에서 "최근 N일" 판정을 세우지 않는다** — 그 날짜는 관측이 아니라 가정이다. **2026-08-05 결론: R4는 폐기했다.** 대체안으로 넣었던 `meta.newCount === 0` 판정도 같은 함정이었다 — 배분 창이 하루 굴러도 평일 집합이 그대로인 날(토·일·월의 창이 08-10~08-14로 동일)이 **매주 2일 고정**으로 생겨 0이 수학적으로 예정된다. 게다가 newCount에는 "창 회전량"이 섞여 있어 새 물건의 척도가 아니다. **갱신 인식은 데이터 채점이 아니라 화면 구조(배지 상시 + 실수집 시각)가 보장한다** — 커버리지는 R1·R2가 이미 지킨다.
- [2026-08-05] 자동 생성 이슈 10건이 열린 채 방치 — #13·#14·#15는 Node 24 수정(fd05d2a)으로 이미 해소된 08-03 실패 건이다 → 닫히지 않는 알림은 쌓여서 다음 진짜 실패를 가린다 — 침묵 실패의 반대편 실패 모드다. **2026-08-05 후속(처방 번복): 닫는 자동화는 두지 않는다.** `gh issue close`는 되돌리기 어려운 부수효과를 자동 실행하고, 제목 접두 매칭이라 사람이 연 이슈까지 닫는다 → 대신 **생성 억제**(같은 접두의 열린 알림이 있으면 만들지 않는다)로 뒤집는다. 단 억제 조회는 반드시 **봇 라벨(`auto-alert`)로 한정**한다 — 라벨 없이 접두만으로 억제하면 옛 이슈·사람이 연 이슈가 억제 계산에 섞여 알림 채널이 통째로 침묵한다(실측: 열린 11건이 3개 접두를 모두 점유). **억제는 침묵의 반대편으로 넘어가기 쉽다 — 억제 범위는 자기가 만든 것으로 좁힌다.**
- [2026-08-05] 갱신을 매일로 올리면 **배분 창이 `nextUpdateAt`에 묶여 하루로 붕괴**한다(cron 한 줄만 바꿔도 산출물이 깨지는 경로) → "다음 갱신일까지 배분"은 갱신이 주 1회일 때만 7일을 뜻했고, 매일 갱신에서는 1,000건이 내일 하루치 기일에 몰려 2026-08-02 결함(다음 날부터 전 물건 기일 경과·시도 5곳 0건)이 그대로 재현된다 → **배분 창을 갱신 주기에서 분리해 상수로 고정**한다(`OUTPUT_WINDOW_DAYS` 7일, 게이트 R2도 같은 상수로 채점). 주기를 바꾸기 전에 그 주기에서 파생된 값을 먼저 훑는다.
- [2026-08-05] 워크플로 YAML 검증에 `require('yaml')`을 썼는데 루트에 없어 MODULE_NOT_FOUND → 이 저장소에 설치된 것은 **`js-yaml`**(transitive)이다 → YAML 파싱 검증은 `require('C:/dev/crazyvalue/node_modules/js-yaml').load(...)`로 한다. 워크플로는 문법이 깨지면 자동화가 통째로 죽으므로 수정마다 파싱 검증이 의무인데, 검증 도구 자체가 매번 걸리면 검증을 건너뛰게 된다.
- [2026-08-05] 배포 반영을 기다리려고 `sleep 45 && curl ...`을 썼다가 하네스가 차단 → 전경 `sleep` 체이닝은 금지다 → **조건 대기는 `until <검사>; do sleep 10; done`을 `run_in_background`로** 돌린다(완료 알림이 온다).
- [2026-08-05] 조건부 캐치업(`데이터 나이 ≥ STALE_DAYS`)이 정기 슬롯 성공 주의 나머지 6일을 통째로 건너뛰어 실질 주 1회였다 → 복구 장치로 만든 조건이 정상 운영에서는 실행을 막는 조건이 됐다 → 슬롯 판정을 "실행 여부"가 아니라 **모드**로 바꾼다(월~토 quick · 일 full).
- [2026-08-05] 커밋·이슈 날짜 라벨이 UTC(`date -u +%F` — `crawl.yml` 커밋 1곳·이슈 3곳)라 항상 하루 전으로 찍혔다(실측: 08-04T19:27Z 런의 게이트 로그는 `KST 2026-08-05`인데 그 뒤 커밋 fe29213 제목은 `data: daily update 2026-08-04`) → 슬롯 18:00Z가 KST로는 다음 날이기 때문 → 라벨을 `TZ=Asia/Seoul date +%F`로 산출한다(러너에 tzdata 있음 · 스텝마다 셸이 분리되고 게이트가 죽으면 그 출력이 비므로 게이트 변수를 재사용하지 않고 각 스텝에서 구한다). **주기·슬롯을 옮기면 그 시각에서 파생된 표시값까지 훑는다** — 같은 날 배분 창 붕괴(`nextUpdateAt` 연동)와 동류다.
- [2026-08-05] "매일 새벽 3시" 확정과 실제 발화가 어긋난다(실측 창은 §1에 병기) → GitHub cron의 지연 발화는 플랫폼 정상 동작이라 슬롯 시각으로 정시를 보장할 수단이 없다 → 시각 약속은 지정값이 아니라 **실측 분포**로 문서화하고, 슬롯 조정은 주인님 결정으로 남긴다. 외부 스케줄러의 "정시"는 사양이 아니라 관측 대상이다.
- [2026-08-05] 배경으로 `build`+E2E가 도는 중에 lint 기준선을 재려고 `git stash push -u`/`pop`을 돌렸다 → 워킹트리를 통째로 갈아끼우는 명령이라 실행 중인 빌드가 엉뚱한 소스를 컴파일할 수 있었다(이번엔 빌드가 stash보다 먼저 끝나 무사) → **배경 검증이 도는 동안 워킹트리를 바꾸는 명령(`stash`·`checkout`·`clean`)을 돌리지 않는다.** 기준선 비교가 필요하면 검증이 끝난 뒤에 하거나 별도 워크트리·클론에서 한다.
- [2026-08-05] 문서 1줄만 바꾼 커밋에서 CI 실패 → `npx playwright install --with-deps`가 도는 `apt-get update`가 러너에 설정된 **제3자 저장소**(`packages.microsoft.com/repos/azure-cli`) 403으로 죽었다(exit 100). 우리 코드와 무관한 인프라 플레이크다 → **CI 실패는 먼저 `gh run view --log-failed`로 실패 지점을 보고 코드/인프라를 가른 뒤 대응한다**(인프라면 `gh run rerun --failed`, 재실행으로도 반복되면 `--with-deps`를 걷어내는 등 구조를 고친다). 커밋 내용과 실패 지점이 무관하면 코드를 뒤지지 않는다.
- [2026-08-05] `npm run lint`가 error 1건으로 exit 1 — 이번 변경 탓으로 오인할 뻔했다 → 실제 위치는 `public/sw.js`(serwist가 만든 **추적되지 않는 빌드 산출물**)이고 HEAD에서도 동일하게 87 problems가 난다 → **lint·게이트 실패는 "이번 변경이 만든 것"인지 기준선(HEAD stash 또는 별도 트리) 대조로 판정한 뒤 보고한다.** 생성 산출물이 린트 대상에 들어오는 구성 자체는 별건으로 남는다.
- [2026-08-09] **(같은 결함 5회 재발 = 하네스 실패)** 방문자 화면 기준일이 옛 날짜에 영구 고착(주인님 폰: 08-07 배포 위에 08-02 표기 · `신규 N건`이 없는 것이 옛 meta 본문이라는 결정적 증거 — 그 필드는 08-05에 생겼다) → 원인 둘이 겹쳤다: ① `sw.ts`가 meta.json을 NetworkFirst **3초**로 두어 망이 느리면 캐시된 옛 날짜가 첫 화면에 올라간다 ② `use-meta.ts`의 `pending` 설계가 새 날짜를 찾고도 **화면 값을 교체하지 않고** 버튼만 띄웠다(근거로 적어둔 "자동 새로고침은 필터·스크롤을 날린다"는 페이지 재적재에 해당하는 말이지 날짜 글자를 바꾸는 것과 무관했다 — 이 혼동이 고착을 영구로 만들었다) → **기준일을 런타임 fetch에서 빼내 빌드 산출물(HTML)로 옮긴다**(`build-meta.ts` · DataDateBar 서버 컴포넌트화). 데이터는 커밋돼 배포와 함께 나가므로 빌드가 이미 crawledAt을 안다 — **받아올 게 없으면 굳을 것도 없다.** 문서는 타임아웃 없는 NetworkFirst라 진짜 오프라인일 때만 캐시로 떨어지고, 그때 보이는 옛 날짜는 그 HTML의 실제 산출 시각이라 거짓이 아니다. **재발이 5회까지 간 진짜 이유는 검증 구멍이다** — 기준일 바 가드가 `\d{2}-\d{2}` 표기 **형식**만 봐서 일주일 묵은 날짜도 통과시켰고, 갱신 워크플로는 커밋된 파일만 jq로 채점하고 배포된 화면을 한 번도 보지 않았다. **형식 가드는 값 가드가 아니다** — `tests/e2e/data-date.spec.ts`로 화면 문자열과 `meta.json` 값을 직접 대조하고, 망을 전부 끊은 상태·옛 캐시를 심은 상태에서도 흔들리지 않음을 단언한다.
- [2026-08-09] 고착 재현에 Playwright `context.route`로 meta.json을 abort·6초 지연시켰으나 둘 다 화면이 정상 복구돼 "재현 실패"로 오판할 뻔했다 → **`route`는 서비스워커 내부 요청을 가로채지 못한다**(6초 지연을 걸었는데 4.5초에 최신값이 떴다 = SW의 fetch는 우회했다는 증거) → **서비스워커 경로를 시험할 때는 `route`가 아니라 `context.setOffline(true)`를 쓴다**(네트워크 스택 수준이라 SW까지 걸린다). 도구가 대상 계층에 닿는지부터 확인하고 재현 결과를 해석한다 — 안 닿는 도구의 "정상"은 정상이 아니라 미측정이다.
- [2026-08-09] 데이터가 08-07로 바뀌자 smoke의 "온보딩→필터 3탭" E2E가 결과 링크를 못 찾고 타임아웃 → 픽스처가 **지역만 동적이고 금액·용도는 하드코딩**(`1~3억` × `상가`)이라, 지역이 부산(60건)→충북(35건)으로 옮겨간 순간 교집합이 0건이 되어 결과 CTA 자체가 사라졌다(실측: 충북 상가 0건, 용도 분포는 토지 19·기타 5·아파트 5뿐) → 원장 2026-08-02 처방("조건을 만족하는 것을 매번 고른다")을 **세 축 전부**에 적용한다(`tests/e2e/fixture.ts` `pickAxes` — 건수 최다 조합, 동수는 카탈로그 순서로 결정). **절반만 적용한 처방은 처방이 아니다** — 한 축을 고정해두면 나머지가 움직이는 순간 같은 결함이 그대로 돌아온다.
- [2026-08-09] 위 파손이 **이틀간 무관측**이었다(08-07 데이터 커밋 시점부터 깨져 있었는데 08-09 사람 푸시에서야 드러났다) → **봇이 `GITHUB_TOKEN`으로 올리는 커밋은 워크플로를 발화시키지 않는다**(GitHub 사양 — 무한 루프 방지). 매일 갱신이 화면을 깨뜨려도 CI가 돌지 않으니 사람이 푸시할 때까지 아무도 모른다 → **자동화가 만든 변경은 그 자동화가 스스로 검사해야 한다.** 갱신 워크플로가 자기 커밋 뒤에 검증(배포 화면 대조 포함)을 직접 돌린다. 사람 푸시에 검사를 얹어두면 며칠씩 밀린다 — 원장 2026-08-02 "검증이 파이프라인 안쪽에서 멈춘다"의 반대편 구멍이다.
- [2026-08-09] **알림 억제가 침묵 실패를 만들었다** — 08-06자 이슈 #17 하나가 접두 억제(`startswith`)에 걸려 08-08·08-09 두 번의 수집 실패를 통째로 삼켰고, 주인님은 이틀간 갱신이 멈춘 사실을 모르셨다 → 억제 단위를 **접두가 아니라 그날 제목 전체**로 좁힌다(`select(.title == $t)`, `TITLE="$PREFIX $(TZ=Asia/Seoul date +%F)"`). 조건이 지속되면 날마다 1건씩 쌓이는데 **그 쌓임 자체가 "며칠째 실패 중"이라는 신호**다. 원장 2026-08-05가 이미 "억제는 침묵의 반대편으로 넘어가기 쉽다"를 남겼는데 라벨 한정만 하고 **시간 축을 안 넣어** 그대로 넘어갔다 — 억제에는 항상 **범위(누가)와 유효기간(언제까지)** 둘 다 건다.
- [2026-08-09] (사전 차단) 새 검증 스텝을 `npx tsx ... | tee log`로 쓸 뻔했다 → GitHub 기본 셸은 `bash -e`라 **pipefail이 없어 파이프라인 종료코드가 `tee`의 것(0)이 된다**(실증: pipefail OFF → exit 0 · ON → exit 3). 그대로 나갔으면 배포 화면 불일치 알림이 영영 발화하지 않는다 → 로그를 파이프로 넘기는 스텝에는 `set -o pipefail`을 붙인다. 원장 2026-07-19와 같은 함정이 **다른 자리에서 재발**했다 — 조문이 "배경 체인"에 묶여 있어 워크플로 스텝에 적용될 줄 몰랐다. **함정은 문맥이 아니라 문법에 붙는다.**
- [2026-08-09] (실측) 한국 IP·로컬에서 `npm run crawl -- --dry-run --limit 1`이 **1.4초·라이브 요청 2회로 세션 취득 성공**했다(11:35 KST). 같은 첫 요청에서 GitHub 미국 러너는 3일 연속 죽었다 → "해외·클라우드 IP 차단" 가설에 유리한 관측이나 **시각이 통제되지 않았다**(실패는 03:2x~03:5x KST). 확정은 `diagnose network` 스텝이 실패 순간에 찍는 계층별 결과로 한다 — TCP 시간초과면 방화벽, ECONNRESET·DNS 실패면 사이트 쪽이다. **유리한 관측 하나로 원인을 단정하지 않는다.**
- [2026-08-09] (실전 검증·성공 기록) 계측 배포 직후 `workflow_dispatch` quick 런이 **성공**했다(11:39 착수 · 세션 즉시 취득 · 요청 158회 · 조기 종료 없음 · 산출 1,000건 / 기일 5일 / 0건 시도 없음 / 신규 17건). **배포 화면 검증 스텝이 실전에서 처음 초록** — 5회차 폴링(약 62초)에 프로덕션 HTML에서 `08-09(일) 11:43`을 확인했다. 화면·데이터 독립 대조도 일치. → **세션 실패의 시각 상관은 여전히 반증됐다**: 성공 04:04·04:26·04:27·08:55·11:41 vs 실패 03:22·03:51·04:25 — **04:25 실패와 04:26 성공이 인접**하므로 시각만으로 갈리지 않는다. 한국 IP 성공(11:35)도 시각이 달라 지리 가설의 증거가 되지 못한다. 현재 상태는 **간헐적·미해명**이 정확하다. 다음 실패 1회에서 `diagnose network`가 계층(TCP 시간초과=방화벽 / ECONNRESET·DNS=사이트)을 가른다. **성공했다고 원인이 사라진 것이 아니다 — 계측을 걷어내지 않는다.**
- [2026-08-11] (원인 확정) 08-10 새벽 실패에서 `diagnose network`가 답을 뱉었다 — DNS는 `203.240.76.40`으로 정상 해석되는데 **TCP 443이 25초 시간초과**(`connect=0.000000s`), Node는 `UND_ERR_CONNECT_TIMEOUT`, 러너 공인 IP `172.214.155.234`(Azure) → **법원 사이트가 GitHub 러너의 패킷을 버린다**(거절이면 즉시 RST가 온다). 간헐성도 설명된다: 런마다 러너 IP가 바뀌고 그중 일부가 차단 대상이다. **처방은 실행 환경을 국내로 옮기는 구조 변경이라 주인님 결정 사항**이며 자동화가 임의로 할 일이 아니다. 계측을 심은 다음 실패 **한 번**에 확정됐다 — 그전 3회는 같은 증상을 겪고도 아무것도 남기지 못했다. **원인을 모르면 계측부터 심는다.**
- [2026-08-11] 게이트 R2가 **만족 불가능한 조건**을 요구했다 — "배분 창 7일 마지막 날까지 기일이 남아야 한다"인데 원천에 그 기일이 없었다(실측: 목록 조회는 서버 총 5,666건 전량 수신·142페이지 정상 종결이고, 창을 08-16으로 연 08-09 런과 08-18로 연 08-11 런의 **마지막 기일이 똑같이 08-14**였다 — 거리가 아니라 고정된 날짜다) → 매일 거짓 위반이 쌓여 진짜 신호를 가린다(원장 2026-08-05 R4 폐기와 같은 계열). **처방: 묻는 것을 바꾼다 — "창을 다 덮었는가"가 아니라 "원천이 준 것을 다 반영했는가".** 그러려면 원천 사실을 기록해야 하는데 meta에 없었다 → `sourceLastSaleDate`·`candidatesByRegion`(둘 다 상한 적용 **전**)을 남기고 R2가 그것을 기준으로 채점한다. **채점기가 원천을 모르면 원천 탓과 우리 탓을 구분할 수 없다 — 게이트에는 비교 대상이 함께 기록돼야 한다.**
- [2026-08-11] 위 R2 교체를 뮤테이션으로 시험하다 **메시지가 원인을 거짓으로 알리는 것**을 잡았다 — 원천이 창을 꽉 채워준 날(`sourceLast == lastUseful`)에 우리가 자르면 사유가 "창이 모자랐다"로 찍혔다. 사유 판별을 요구 기준(`sourceLast < lastUseful`)에 묶은 탓이다 → **사유는 요구 기준이 아니라 원천 대비 실제 산출(`maxDate < sourceLast`)로 판별**한다. **위반이 뜬다고 게이트가 옳은 것이 아니다** — 통과/실패뿐 아니라 **사유 문구까지** 뮤테이션으로 검사한다(잘못된 사유는 다음 사람의 조사 방향을 통째로 돌린다).
- [2026-08-11] **(2차 확증 · 시각 가설 최종 반증)** 게이트 수정 뒤 실전 등가 dispatch가 **저녁 20:11 KST**에 실패했다 — 러너 IP `20.118.214.18`(08-10은 `172.214.155.234`, 둘 다 Azure), DNS는 `203.240.76.40`으로 정상, TCP 443은 `exit 124`, curl은 `connect=0.000000s`로 25초 시간초과, Node는 10회 시도 전부 `UND_ERR_CONNECT_TIMEOUT`. **같은 날 03:57 스케줄 런은 성공했다** → 새벽/저녁 어느 쪽도 아니고 **러너 IP가 갈린다**. 시각 상관은 이제 완전히 죽었다. → 처방은 실행 환경을 국내로 옮기는 구조 변경이며 **주인님 결정 사항**이다. **부수 소득**: ① `describeError`가 실전에서 작동해 로그가 `fetch failed <- ConnectTimeoutError (code=UND_ERR_CONNECT_TIMEOUT)`로 남았다(예전엔 껍데기만) ② 억제를 그날 제목으로 좁힌 수정이 실증됐다 — 옛 이슈 #17·#18이 열려 있는데도 오늘치 #20이 정상 생성됐다 ③ 무커밋 계약이 지켜져 프로덕션은 08-11 03:57 그대로다. **실패한 검증도 산출물이다** — 실패 한 번이 가설 하나를 죽이고 수정 세 개를 증명했다.
- [2026-08-11] (실전 확정) 위 실패 직후 같은 dispatch를 **한 번 더** 돌리자 성공했다(20:19 · 러너 IP만 바뀌었다 — IP 가설의 세 번째 증거다). 새 필드가 생성돼 R2 교체가 프로덕션에서 검증됐다: `원천 최종 기일 2026-08-14 · 위반 0건`. 08-11 03:57 산출에서 나던 R2 위반은 **거짓 경보였음이 확정**됐다 — 산출도 08-14, 원천도 08-14였다. **그리고 전북 0건의 원인이 갈렸다: 원천 후보 1건 → 산출 0건, 즉 상한 배분이 지웠다.** 전북은 그 매각기일의 저가순 쿼터에 단 1건이 못 들었다. **이것은 원천 탓이 아니라 우리 손해다** — 소수 지역이 상한에 통째로 지워지지 않도록 배분을 보완할 대상이며(별건), 게이트는 그 사실을 `0건 시도 1개(상한이 지운 것 1개: jeonbuk)`로 상시 표면화한다. **원천을 기록하기 전에는 이 두 사실 중 어느 것도 말할 수 없었다** — 같은 "0건"이 원천 탓일 수도 우리 탓일 수도 있었고, 구분할 값이 없어 매번 추측으로 끝났다.
- [2026-08-11] **(IP 차단 처방)** "실패하면 재시도"를 늘리는 것은 이 결함에 무력하다 — **러너 공인 IP는 job마다 고정**이라 같은 job 안에서 몇 번을 돌려도 같은 차단 IP다(실측: 창 50분·10회 재시도가 전부 같은 `UND_ERR_CONNECT_TIMEOUT`으로 죽었고, 8분 뒤 **새 런**이 다른 IP를 받아 즉시 성공했다) → 시도를 **matrix job으로 나눈다**(`fail-fast: false` · `max-parallel: 1` · attempt 1·2·3). 시도마다 새 러너 = 새 IP다. 앞 시도가 오늘치를 올렸으면 뒤 시도는 슬롯 판정에서 즉시 건너뛰고(중복 수집 0), 수동 실행(리프레쉬)은 1회차로 끝낸다(방문자 대기 "10분 안팎" 약속 보호). 같은 job 안의 창은 50분 → **600초**로 줄였다 — 길게 버티는 것이 순수 낭비임이 드러났기 때문이다. 실패 알림은 **마지막 시도에서만** 낸다(1회차가 죽고 2회차가 살아난 날에 "실패" 알림이 뜨면 신호가 신뢰를 잃는다). **재시도 설계는 "몇 번 더"가 아니라 "무엇이 바뀌는가"로 정한다** — 바뀌는 것이 없으면 반복은 대기 시간일 뿐이다.
- [2026-08-11] (사전 차단) 위 건너뛰기 분기를 `[ 조건 ] && skip "..."`로 썼다 → GitHub 기본 셸은 `bash -e`라 조건이 거짓이면 AND-OR 리스트가 non-zero를 반환해 스텝이 통째로 실패할 수 있다(그러면 성공한 날에 진단·알림이 발화한다). **실증으로 확인했다** — 5개 분기 전부 exit 0으로, POSIX가 AND-OR 리스트의 마지막이 아닌 명령에는 `-e`를 적용하지 않기 때문이다. 통과했지만 **셸 함정은 추측으로 넘기지 않고 그 자리에서 실행해 확인한다**(원장 2026-07-19 pipefail·2026-08-09 tee와 같은 계열 — 셸의 종료코드 규칙은 직관과 자주 어긋난다).
- [2026-08-11] **(자기 수정이 만든 침묵 실패)** 실패 알림을 `matrix.attempt == 3`으로 좁혔는데, 수동 실행은 2·3회차를 **설계상 건너뛰므로** 그 조건이 **영원히 성립하지 않아** 리프레쉬 실패가 통째로 침묵했다(실측: 20:51 수동 런이 실패했는데 알림 경로가 아예 안 탔다) → 조건에 `|| github.event_name == 'workflow_dispatch'`를 더한다. **시도를 나누면 "마지막 시도"의 정의가 트리거마다 달라진다** — 알림 조건을 시도 번호에 묶을 때는 **그 번호에 도달하지 못하는 경로가 있는지** 먼저 센다. 하루에 알림 침묵을 두 번(억제 범위 · 시도 번호) 서로 다른 원인으로 만들었다: **침묵 실패는 한 곳을 막으면 다른 곳에서 난다 — 고칠 때마다 "이 경로로 실패하면 누가 알게 되는가"를 끝까지 따라간다.**
- [2026-08-11] (관측 누적) 차단된 러너 IP 3개 확인 — `172.214.155.234`(08-10 04:24) · `20.118.214.18`(08-11 20:11) · `20.81.159.5`(08-11 20:50). 전부 Azure이고 전부 `TCP 443 exit 124`. 같은 날 03:57·20:19 런은 다른 IP로 성공했다. **차단은 대역 전체가 아니라 IP 단위이고 무작위로 걸린다** — 그래서 새 러너를 받는 것이 유일하게 작동하는 회피다.

## 5. 방문자 감사 백로그 (2026-07-18 1차 — 순번 고정)
> 근거: 방문자 Flow 병렬 감사 5영역(온보딩·필터/리스트·상세 액션·관심함 재방문·탭/백링크) 발견 32건 → 중복 통합·그룹 분할 후 30건. 순번은 이후 라운드에서 불변. 확정 사양(무가입·주1회 갱신·사진 플레이스홀더·목데이터)은 결함 아님. Lighthouse P점수(TBT) 개선은 기지(旣知) defer 항목.

| 순번 | 심각도 | 제목 | 수정 방침 | 그룹 | 상태 |
|---|---|---|---|---|---|
| 1 | 불안정 | localStorage 형태 손상 시 관심함·홈·상세 전면 크래시(복구 없이 영구 반복) | watchlist.ts safeRead에 형태 가드(items 순수 객체·snapshot 3필드·ids 배열 검증, 불일치 시 기본값 치환 저장) + vitest 손상 케이스 3종 | watch | 완료(2026-07-19 검증 그린) |
| 2 | 불안정 | 클라이언트 예외 시 영문 전면 에러로 앱 셸 소멸(error.tsx 부재) | src/app/error.tsx 신설 — 앱 셸 유지 한국어 오류 화면 + 다시 시도 동선 | nav | 완료(2026-07-19 검증 그린) |
| 3 | 불안정 | 홈 필터 useState 휘발 — 리스트·상세 뒤로가기 복귀 시 선택·건수 전부 초기화(감사 3건 중복 보고) | 홈 필터를 sessionStorage(+URL 쿼리)로 승격, parseListQuery/buildListQuery 재사용해 재마운트 복원 | home | 완료(2026-07-19 검증 그린) |
| 4 | 불안정 | 온보딩 모달 포커스 트랩·배경 inert·Escape 전무 — 키보드 배경 이탈 시 선택 유실·미완료 잔존 | 열림 시 내부 포커스 이동 + 배경 inert + Escape=건너뛰기 동작(키보드 이탈 결함 동일 수정으로 해소) | onboarding | 완료(2026-07-19 검증 그린) |
| 5 | 접근성 | 필터 조작 시 결과 건수 갱신이 스크린리더에 무공지 | ResultButton 건수 텍스트를 aria-live=polite 영역으로 | home | 완료(2026-07-19 검증 그린) |
| 6 | 접근성 | 시도 선택 시 시군구 칩 등장이 스크린리더에 무공지 | RegionFilter 시군구 컨테이너에 role=status·등장 안내 텍스트 | core-lib | 완료(2026-07-19 검증 그린) |
| 7 | 접근성 | 정렬 select 13px — iOS 포커스 시 강제 화면 확대 | ItemList select 글꼴 16px 이상 적용 | list | 완료(2026-07-19 검증 그린) |
| 8 | 접근성 | 카드 감정가 취소선 대비 3.17:1(전 카드 공통, 기준 4.5:1) | ItemCard 취소선 text-ink/50→/70 상향(list·watch 공용 카드) | list | 완료(2026-07-19 검증 그린) |
| 9 | 접근성 | 상세 ink/60 텍스트 3곳 대비 4.19~4.25:1(사건번호 줄 포함) | 상세·PriceStructure의 text-ink/60→/70 상향 | detail | 완료(2026-07-19 검증 그린) |
| 10 | 접근성 | 관심함 기기 저장 고지 대비 4.19:1 | watch/page.tsx 고지 text-ink/60→/70 상향 | watch | 완료(2026-07-19 검증 그린) |
| 11 | 접근성 | 상태 배지가 카드 링크 접근 이름에 미포함·제거 버튼 접근 이름 전부 동일 | WatchCard 링크 내 sr-only 상태 텍스트 + 제거 버튼 aria-label에 물건 식별 병기 | watch | 완료(2026-07-19 검증 그린) |
| 12 | 편의성 | 기본 정렬(기일 임박순) 최상단이 입찰 불가 기일 경과 7건 | sortItems date 분기에서 dday<0 후순위 배치 + 단위 테스트 | core-lib | 완료(2026-07-19 검증 그린) |
| 13 | 편의성 | 만료·오류 URL이 영문 기본 404 — 공유 유입 막다른 길(감사 2건 중복 보고) | src/app/not-found.tsx 신설 — 한국어 안내(갱신 소멸 가능성 1줄) + 홈·리스트 CTA | nav | 완료(2026-07-19 검증 그린) |
| 14 | 편의성 | 동명 시군구 칩 병합으로 선택 범위 암묵 확장(대구 북구→광주 북구 자동 포함) | 시군구를 시도 결합 키로 저장·필터·표기(칩에 시도 병기, d 파라미터 인코딩 변경) | core-lib | 완료(2026-07-19 검증 그린) |
| 15 | 편의성 | 리스트에 적용 중 필터 표시 없음(픽 배너만 존재) | parseListQuery 결과를 상단 요약 칩으로 렌더(개별 해제 시 URL 갱신) | list | 완료(2026-07-19 검증 그린) |
| 16 | 편의성 | /list·/item에서 하단 탭 활성 표시 전무 | BottomTabs 활성 판정에 /list·/item→홈 탭 매핑 | nav | 완료(2026-07-19 검증 그린) |
| 17 | 편의성 | 관심함 변화 배지가 렌더 1회로 소멸(상세 왕복만으로 유실) | 판정 diff를 sessionStorage 세션 캐시로 유지 + 배지에 이전→현재 최저가 병기 | watch | 완료(2026-07-19 검증 그린) |
| 18 | 편의성 | 설치형(standalone) 상세 화면에 가시적 뒤로가기 부재 | 상세 상단 뒤로가기 버튼(history 없으면 /list 폴백) | detail | 완료(2026-07-19 검증 그린) |
| 19 | 편의성 | 온보딩 이후 관심조건 변경·재설정 경로 전무 | /guide에 관심조건 편집 섹션(setPrefs 재사용, 저장 즉시 반영) | nav | 완료(2026-07-19 검증 그린) |
| 20 | 편의성 | 유찰 타임라인이 이번 회차·현재 최저가로 미연결 | HistoryTimeline 말미에 saleDate·minPrice 파생 "이번 회차(예정)" 행 추가 | detail | 완료(2026-07-19 검증 그린) |
| 21 | 편의성 | 매각 종료 카드에 물건 식별 정보 부재(카드 간 구별 불가) | 1차: id 파싱해 지역·사건번호 표기, 2차: snapshot에 address·category 확장 | watch | 완료(2026-07-19 검증 그린) |
| 22 | 편의성 | 기일 경과 카드 제거 수단 부재 + 기일 경과 문구 중복 표기 | expired 카드에 제거 버튼 노출(종료 카드와 대칭) + 문구 1회만 표기 | watch | 완료(2026-07-19 검증 그린) |
| 23 | 편의성 | 관심조건 선택 후 건너뛰기 탭 시 무경고 폐기(복구 불가) | 선택 1개 이상이면 폐기 확인 1줄 표시 후 진행 | onboarding | 완료(2026-07-19 검증 그린) |
| 24 | 편의성 | 온보딩 열림 중 배경 스크롤 미잠금(스크롤 체이닝) | open 동안 body 스크롤 잠금 + 시트 overscroll-contain | onboarding | 완료(2026-07-19 검증 그린) |
| 25 | 편의성 | 빈 결과 완화 버튼(금액 범위 넓히기)이 조건 따라 완전 무동작 | 완화 버튼 조건부 노출(해당 필터 존재 시), 둘 다 없으면 용도 해제 제안 | list | 완료(2026-07-19 검증 그린) |
| 26 | 편의성 | 인근 지역 포함 라벨과 실동작(지역 전체 해제=전국) 불일치 | 라벨을 "지역 조건 해제(전국 보기)"로 정정 | list | 완료(2026-07-19 검증 그린) |
| 27 | 편의성 | 클립보드 차단 환경에서 공유 실패 시 무피드백 | ShareButton catch 분기 — AbortError만 무시, 실패 라벨 aria-live 표시 + URL 텍스트 폴백 | detail | 완료(2026-07-19 검증 그린) |
| 28 | 성능 | 재방문 저속망에서 온보딩 시트 약 233ms 플래시(beforeInteractive 실행 미보장) | 외부 onboarding-flag.js 폐기, layout.tsx head 인라인 스크립트로 페인트 전 실행 보장 | onboarding | 완료(2026-07-19 검증 그린) |
| 29 | 성능 | 더보기·정렬 탭마다 서버 RSC 왕복(672~1243ms) 후 화면 반영 | n·sort 변경을 셸로우 URL 갱신(history.replaceState)+로컬 상태로 처리해 왕복 제거 | list | 완료(2026-07-19 검증 그린) |
| 30 | 성능 | 관심함 진입 시 관심 건수 무관 전 지역 18파일 로드 | watchlist id 지역 접두 파싱해 필요 파일만 로드 | defer | 보류(현 규모 체감 결함 아님 — Phase 4 서비스워커 /data 캐싱과 함께 재평가) |

- 미실시 감사 2영역(복원력·문구/접근성 전수) → §5-2 2차 라운드에서 35셀 매트릭스로 흡수·해소.

## 5-2. 방문자 감사 백로그 2차 (2026-07-22 — 순번 31~ 고정)
> 범위·판정은 §2-2 헌법. 착수 전 확정된 기지(旣知) 결함 4건(A 목데이터 법원 링크 루트 고정 · B `scripts/crawl.ts` 미커밋으로 주간 갱신 파이프라인 파손 · C 갱신 지연 미표기 · D Phase 4 미커밋·설치 배너 겹침)은 감사 결과와 무관하게 수정 대상이다.
> 승인 신설 사양 5건: N1 내 설정(`/me`) · N2 이탈 확인 팝업 · N3 법원 링크 3종 · N4 주간 갱신 복구 · N5 Phase 4 마감.

> 감사 실적(2026-07-22): 7트랙 × 5축 **35/35셀 기입** · 원발견 62건 → 적대적 검증 **확정 58 · 폐기 4** · 완결성 비평가 추가 8건 = **66건** → 교차 트랙 중복 통합 후 **순번 31~93(63건)**. 12기수·소요 128분.
> 검증 스펙은 `tests/e2e/audit-*.spec.ts`(마감 시 삭제). 폐기 4건은 재현 실패·확정 사양 오인으로 등재하지 않는다.

| 순번 | 심각도 | 셀 | 제목 | 수정 방침 | 그룹 | 상태 |
|---|---|---|---|---|---|---|
| 31 | 불안정 | J5-Q1 | 저장 실패(용량 초과·프라이빗)에도 성공 상태 표시 — 관심등록·온보딩·관심조건 3곳 공통, 실제 저장 0 | watchlist 쓰기 함수가 성공 여부를 반환하고 호출부 3곳이 실패를 표시 | me-core | 완료 |
| 32 | 정합성 | J2-Q3 | 홈 세션 미러가 prefs보다 우선 — 홈 필터를 한 번이라도 만진 세션에서 관심조건 변경이 영구 미반영(안내는 반영된다고 표기) | 복원 우선순위를 URL→prefs 변경시각→세션으로 교정, prefs 저장 시 세션 미러 무효화 | me-core | 완료 |
| 33 | 정합성 | J3-Q1 | 두 탭 동시 편집 시 나중 저장이 앞 저장을 통째 덮어씀 + 앞 화면은 옛 상태 유지 | storage 이벤트 구독으로 타 탭 변경 반영, prefs 필드 단위 병합 | me-core | 완료 |
| 34 | 정합성 | J3-Q1 | 관심조건 칩 1회 탭이 onboarded=true를 세워 온보딩을 영구 미노출 | 온보딩 완료 플래그는 온보딩 시트에서만 기록 | me-screen | 완료 |
| 35 | 정합성 | J5-Q1 | 형태 가드가 saleDate를 typeof string으로만 검사 — 손상 값 렌더·화면에 `(undefined)` 노출 | 날짜 형식(YYYY-MM-DD) 검증 추가, 불일치 시 항목 폐기 + 회귀 테스트 | me-core | 완료 |
| 36 | 정합성 | J4-Q3 | 원천 JSON 손상 시 검증 없이 거짓 사실 단정 표기(픽 배지·할인율·금액) | 클라이언트 로드 시 zod 파싱 실패 항목 제외 + 제외 건수 고지 | data | 완료 |
| 37 | 정합성 | J4-Q3 | 존재하지 않는 매각기일이 조용히 실제 날짜로 이월돼 유효한 D-day로 표기 | format.ts 날짜 파싱을 엄격 검증(roll-over 금지)으로 교정 | data | 완료 |
| 38 | 정합성 | J6-Q3 | 기준일 바가 수집 시각을 리터럴 `03:00`으로 하드코딩 — crawledAt 실시각과 무관하게 항상 03:00 | crawledAt의 실제 시각을 포맷해 표기 | data | 완료 |
| 39 | 정합성 | J1-Q1 | 데이터 로드 실패 후 복구해도 기준일 바 오류가 세션 내내 잔존(전체 새로고침에서만 회복) | useMeta에 재시도 경로 연결 — 본문 "다시 시도"가 meta도 재요청 | data | 완료 |
| 40 | 정합성 | J1-Q3 | "이번 주 신규"가 10일 지난 주간을 "이번 주"로 표기 — 13~16일 전 유찰 물건을 최신으로 오인시킴 | 판정창을 오늘 기준으로 산정하고, 갱신 지연 시 라벨을 기준일 병기로 전환 | data | 완료 |
| 41 | 정합성 | J6-Q3 | 열린 세션에서 데이터·기준일을 한 번도 재검증하지 않아 설치형 PWA가 무기한 낡은 화면 유지 | visibilitychange 복귀 시 meta 재검증 + 변경 시 갱신 안내 | data | 완료 |
| 42 | 정합성 | J6-Q3 | 주간 크롤 워크플로에 실패 알림·동시성 가드·타임아웃 전무 — 실패해도 아무도 모름 | crawl.yml에 concurrency·timeout-minutes·실패 시 이슈 생성 추가 | data | 완료 |
| 43 | 정합성 | J6-Q5 | 서비스워커가 갱신 직후 첫 재방문에 지난주 데이터·기준일을 최신으로 제공 | /data 요청을 NetworkFirst(짧은 타임아웃)로 전환하고 meta는 캐시 우회 | pwa | 완료 |
| 44 | 정합성 | J4-Q1 | parseListQuery가 지역(r)을 화이트리스트 검증하지 않고 4축 중복도 미제거 — 유령 칩·중복 칩 렌더 | r 화이트리스트 검증 + 4축 dedupe + 단위 테스트 | list-nav | 완료 |
| 45 | 정합성 | J4-Q3 | **핵심목표 미달** — 법원 원문 도달 3종 중 ②사건번호 복사·③찾는 방법 안내가 앱 어디에도 없음 | 상세에 사건번호 복사 버튼 + 찾는 방법 1줄 신설(N3) | detail-link | 완료 |
| 46 | 정합성 | J4-Q3 | 기일 경과 물건 타임라인 말미가 "이번 회차(예정)" — 같은 화면 "남은 기간: 기일 경과"와 모순 | dday<0이면 예정 행 미표기 또는 "기일 경과"로 라벨 전환 | detail-link | 완료 |
| 47 | 편의성 | J1-Q2 | 온보딩 시트 주 CTA 2개가 첫 페인트 가시 높이 0px(162px 스크롤해야 등장) — 최초 방문에 진행 수단 없음 | 시트 레이아웃을 헤더/스크롤 본문/고정 푸터로 분리해 CTA 상시 노출 | onboarding | 완료 |
| 48 | 편의성 | J2-Q2 | 건너뛴 방문자에게 나중 설정 가능 신호 부재 + 선택 개수 피드백이 폐기 확인문에만 등장 | 시트에 재설정 위치 1줄 + 선택 개수 상시 표시 | onboarding | 완료 |
| 49 | 편의성 | J2-Q4 | 시트 안내 3문단이 전부 2문장 병렬(세로 리듬 위반)인데 게이트 R12가 습니다체만 검사해 단정형을 구조적으로 미검출 | 문단 1문장 압축 + 게이트 R12를 문체 불문 문장 수 판정으로 교정 | onboarding | 완료 |
| 50 | 편의성 | J1-Q2 | 온보딩 건너뛴 첫 화면에 앱 목적 문구가 11px 태그라인뿐 — 용어 설명은 화면 밖 | 홈 상단에 1줄 목적 문구(기존 태그라인 승격, 중복 신설 금지) | a11y-global | 완료 |
| 51 | 편의성 | J3-Q2 | 하단 탭·페이지 제목 어디에도 "내 설정" 단서가 없어 관심조건 편집 화면을 찾을 수 없음 | `/me` 신설 + 하단 탭 4번째 "내 설정"(N1) | me-screen | 완료 |
| 52 | 편의성 | J3-Q2 | 자기인식 지표(관심함 건수·최근 본 건수·현재 적용 조건 요약) 전무 | `/me`에 저장 현황 블록(N1) | me-screen | 완료 |
| 53 | 편의성 | J3-Q2 | 내 데이터 초기화(무가입 앱의 로그아웃 대응) 수단이 앱 전체에 없음 | `watchlist.resetAll()` + `/me` 초기화(확인 경유)(N1) | me-screen | 완료 |
| 54 | 편의성 | J4-Q1 | 리스트 "더보기" 더블탭 시 두 번째 탭이 새로 삽입된 카드에 적중해 엉뚱한 상세로 이탈 | 더보기 후 삽입 영역 클릭 차단(리스트 앵커 유지) 또는 버튼 위치 고정 | list-nav | 완료 |
| 55 | 편의성 | J4-Q2 | 리스트에서 해제한 지역이 뒤로가기 홈 복귀 시 되살아나 홈·리스트가 서로 다른 건수를 주장 | 리스트의 필터 변경을 홈 세션 미러에 동기화 | list-nav | 완료 |
| 56 | 편의성 | J4-Q2 | pick=1 전용 모드를 리스트에서 해제할 수단이 없고 "적용 중 필터"에도 미표시 | 픽을 해제 가능한 요약 칩으로 편입 | list-nav | 완료 |
| 57 | 편의성 | J4-Q2 | 더보기로 펼친 목록이 정렬 변경만으로 10건으로 되접힘 | 정렬 변경 시 n 유지 | list-nav | 완료 |
| 58 | 편의성 | J5-Q2 | 기일이 남은 정상 관심 카드에 제거 버튼이 없어 상세 왕복이 강제됨 | 전 카드에 제거 버튼 노출(종료·경과 카드와 대칭) | watch | 완료 |
| 59 | 편의성 | J5-Q2 | 입찰 불가한 기일 경과 물건이 관심함 최상단 — 백로그 12 후순위 규칙이 관심함 비교자에 미적용 | 관심함 정렬에 dday<0 후순위 규칙 재사용 | watch | 완료 |
| 60 | 편의성 | J5-Q2 | 관심함 제거가 확인·되돌리기 없이 즉시 영구 삭제(오탭 복구 불가) | 제거 후 되돌리기 1회 제공 | watch | 완료 |
| 61 | 편의성 | J5-Q2 | 빈 관심함 카드가 래퍼 p-4 + EmptyState m-4 이중 여백으로 타 화면 대비 32px 좁게 렌더 | 이중 여백 해소(레이어 간 패딩 중복 금지 규칙) | watch | 완료 |
| 62 | 편의성 | J5-Q3 | "기일 변경" 배지의 날짜만 formatDateKr을 경유하지 않아 요일 없는 원시 ISO로 표기 | format.ts 단일 경유 규칙 적용 | watch | 완료 |
| 63 | 편의성 | J5-Q4 | 같은 오류 상황인데 관심함만 습니다체 — 앱 문체가 화면마다 갈림 | 단정형으로 통일 + 게이트 문체 룰 연동(49번과 동반) | watch | 완료 |
| 64 | 편의성 | J6-Q2 | 관심 물건의 재유찰·기일 변경이 재방문 첫 화면·탭 어디에도 안 드러나 관심함을 직접 열어야 앎 | 하단 탭 관심함에 변화 건수 배지 | watch | 완료 |
| 65 | 편의성 | J6-Q5 | 오프라인에서 미캐시 상세가 무반응 실패하고 오프라인 상태 자체를 고지하지 않음 | 오프라인 폴백 화면 + 온라인 복귀 감지 안내 | pwa | 완료 |
| 66 | 편의성 | J6-Q5 | PWA 설치 아이콘 미완비 — maskable 아이콘도 apple-touch-icon도 없음 | gen-icons로 maskable·apple-touch-icon 생성 + manifest·layout 반영 | pwa | 완료 |
| 67 | 편의성 | J7-Q2 | 상세 "뒤로" 폴백 판정(history.length>1)이 앱 내 이력을 반영 못해 외부 유입자가 앱 밖으로 이탈 | 앱 내 내비게이션 플래그(sessionStorage)로 폴백 판정 교체 | exit-shell | 완료 |
| 68 | 편의성 | J1-Q1 | 데이터 로드 실패를 기준일 바와 본문 ErrorState가 2중 안내(1정보 1표시 위반) | 실패 안내는 본문 1곳으로, 기준일 바는 값 미표기만 | data | 완료 |
| 69 | 편의성 | J7-Q2 | 이탈·종료 확인 수단 부재 — 저장 없이 떠나면 탐색 결과가 전부 소멸(감사에서 부재 확인) | 홈 뒤로가기 확인 시트 + 조건부 beforeunload(N2) | exit-shell | 완료 |
| 70 | 접근성 | J1-Q4 | 온보딩 열림 중 설치 배너만 inert 미적용 — 배너가 effect로 늦게 마운트돼 배경 비활성 계약(백로그 4) 붕괴 | inert 적용을 마운트 시점 무관하게(속성 기반) 재구성 | exit-shell | 완료 |
| 71 | 접근성 | J1-Q4 | 최초 화면 텍스트 6곳 대비 4.5:1 미달(필터 보조 3.12·온보딩 보조 4.19·로딩 라벨 3.65) | 해당 토큰 상향(1차 라운드 9·10과 동일 처방을 누락 지점에 적용) | a11y-global | 완료 |
| 72 | 접근성 | J2-Q4 | 온보딩 시트 텍스트 2곳 4.19:1 — 1차 9·10에서 상세·관심함만 상향하고 시트를 누락 | text-ink/60→/70 | onboarding | 완료 |
| 73 | 접근성 | J1-Q4 | 홈에 h1이 없어 제목 개요가 성립하지 않음 | 홈 h1 신설(시각 규격 유지) | a11y-global | 완료 |
| 74 | 접근성 | J3-Q4 | /guide에 h1 부재 + section aria-label과 h2가 동일 문자열이라 이중 낭독 | h1 신설 + 중복 aria-label 제거 | me-screen | 완료 |
| 75 | 접근성 | J1-Q4 | 헤더 워드마크(전 화면 유일 홈 백링크) 터치타깃 124×28px — 44px 미달 | 헤더 링크 히트영역 44px 확보 | exit-shell | 완료 |
| 76 | 접근성 | J1-Q4 | 본문 글꼴이 px 고정이라 사용자 브라우저·OS 글꼴 크기 설정이 전혀 반영 안 됨 | 본문 타이포를 rem 기반으로 전환(디자인 토큰 경유) | a11y-global | **보류** — 전역 타이포 rem 전환은 41개 UI 파일의 px 리터럴을 일괄 바꿔야 해 세로 리듬 실측치(홈 1127px 등)가 전부 무효화된다. 재개 조건: MASTER.md에 rem 스케일 토큰을 먼저 확정하고 세로 리듬 기준값을 재측정한 뒤 단독 라운드로. |
| 77 | 접근성 | J1-Q4 | 고대비(forced-colors) 모드에서 선택 칩·픽 배지 상태색이 붕괴 | `@media (forced-colors: active)` 규칙 추가 | a11y-global | 완료 |
| 78 | 접근성 | J1-Q4 | 주 CTA 도달에 Tab 36회, 건너뛸 수단 0개 | 본문 바로가기(skip link) 추가 | a11y-global | 완료 |
| 79 | 접근성 | J2-Q4 | 시트에 aria-describedby 부재 + 초기 포커스가 첫 지역 칩 — 브랜드·픽 기준 설명을 낭독 없이 건너뜀 | aria-describedby 연결 + 초기 포커스를 제목으로 | onboarding | 완료 |
| 80 | 접근성 | J3-Q4 | 관심조건 비선택 칩이 흰 카드 위 흰 배경 + 테두리 1.23:1 — 컨트롤 경계 식별 불가(WCAG 1.4.11) | 비선택 칩 테두리 대비 3:1 이상으로 | me-screen | 완료 |
| 81 | 접근성 | J3-Q4 | 칩 묶음이 h3와 프로그래매틱 미연결 — 홈 필터(fieldset+legend)와 달리 소속을 알리지 못함 | fieldset+legend 또는 group+aria-labelledby로 통일 | me-screen | 완료 |
| 82 | 접근성 | J4-Q4 | 상세 외부 링크 3종이 target=_blank인데 새 창 열림 고지가 접근 이름·시각 어디에도 없음 | 접근 이름에 "새 창" 병기 | detail-link | 완료 |
| 83 | 접근성 | J4-Q4 | 리스트 더보기 카운트 대비 4.25:1 | 토큰 상향 | list-nav | 완료 |
| 84 | 접근성 | J4-Q4 | 카드 접근 이름에 두 금액의 의미 라벨이 없어 감정가·최저가 구별 불가 | sr-only 라벨 병기 | list-nav | 완료 |
| 85 | 접근성 | J4-Q4 | 재매각 사건 보증금 문구가 "재매각"을 두 번 말하고 2문장으로 늘어짐 | 1문장 압축·중복 제거 | detail-link | 완료 |
| 86 | 접근성 | J6-Q4 | 기준일 바 로드 실패 문구가 role=alert 없이 조용히 표시되고 복구 동선도 없음 | 39·68번과 함께 처리 | data | 완료 |
| 87 | 성능 | J1-Q5 | 4G 모사에서 결과 건수 표시가 2.84초 지연 — 전송 471KB 중 폰트 262KB(10파일)가 대역 선점 | Pretendard 서브셋 축소·preload 범위 축소로 데이터 요청 선행 | a11y-global | **보류** — 폰트 262KB 축소는 Pretendard 서브셋 재생성이 필요하고 한글 글리프 누락 시 전 화면이 깨진다. 재개 조건: 실사용 글리프 집합을 산출해 서브셋을 만들고 17개 화면 시각 회귀 대조 후. |
| 88 | 성능 | J3-Q5 | 저장된 관심조건이 진입 후 212ms 동안 전부 "미선택"으로 표시 | 하이드레이션 전 상태 반영 또는 스켈레톤 처리 | me-screen | 완료 |
| 89 | 성능 | J4-Q5 | 목록 끝까지 스크롤 시 상세 RSC 프리페치 119건(약 2.4MB) 발생 | 카드 링크 prefetch 비활성 또는 뷰포트 상한 적용 | list-nav | **보류** — prefetch 비활성은 상세 진입 139ms(백로그 29 성과)를 되돌린다. 재개 조건: 뷰포트 상한 prefetch를 구현해 두 지표를 함께 측정한 뒤 결정. |
| 90 | 정합성 | J4-Q3 | **기지 A** — 목데이터 detailUrl이 전부 courtauction.go.kr 루트로 고정(크롤러 DETAIL_URL 규격과 불일치) | gen-mock·전 지역 JSON을 DETAIL_URL 규격으로 통일(N3) | detail-link | 완료 |
| 91 | 불안정 | J6-Q3 | **기지 B** — `scripts/crawl.ts` 미커밋으로 주간 갱신 파이프라인 파손(Actions에서 npm run crawl 실패) | crawl.ts 커밋 + `--dry-run` 실검증(N4) | data | 완료 |
| 92 | 정합성 | J6-Q3 | **기지 C** — 다음 갱신 예정일이 지났는데 지연 표기 없음 | 기준일 바에 갱신 지연 표기(N4) | data | 완료 |
| 93 | 편의성 | J7-Q5 | **기지 D** — Phase 4 미커밋 + 설치 배너가 결과 버튼과 겹침 | 배너 오프셋 재배치 + Phase 4 일괄 마감(N5) | exit-shell·pwa | 완료 |

## 5-3. 방문자 감사 3차 라운드 헌법 (2026-08-17 주인님 승인 — 순번 94~ 고정)

> 근거 지시: "방문자 사용성 전수 Test — 불안정성·불편성을 빠짐없이 발굴하고 개선안까지 완결한다."
> 범위·매트릭스·순번 규칙은 **§2-2를 그대로 승계한다.** 여기서 재정의하지 않는다. 이 절이 새로 고정하는 것은 **판정 방식**과 **단계 순서** 둘뿐이다.

### 1. 주인님 지시 Flow ↔ 매트릭스 사상 (신규 축 신설 금지)

| 지시 Flow | 매트릭스 | 비고 |
|---|---|---|
| ① 앱 방문(첫 진입) | J1 | |
| ② 로그인 / 회원 등록 | J2 | §2-2 5번에 따라 **관심조건 등록**으로 사상(무가입 정체성) |
| ③ 관심사항 등록·저장 | J2·J5 | |
| ④ 방문자 맞춤 설정 | J3 | |
| ⑤ 재방문 시 맞춤화면 자동 열림 | J6 | **영속성 = 저장이 아니라 "열림"까지 판정한다** |
| ⑥ 로그아웃 · 탭 닫기 팝업 | J7 | 로그아웃 = **내 데이터 초기화**로 사상 |
| ⑦ 각 탭 기능 · 백링크 | J7 | 홈탭·닫기 팝업은 **작동 유·무를 먼저 확정한 뒤** 설정을 검토한다 |

### 2. 셀 판정 방식은 2종이며, 기계 판정에는 자격 시험이 있다

§2-2 1번의 "35셀 전수"는 유지한다. 다만 **사람이 도는 셀과 기계가 채점하는 셀을 가른다.**

- **기계 판정 셀** — 회귀 자산(유닛 141 · E2E 22)이 그 동작을 지키는 셀. 판정 = 테스트 실행.
- **실브라우저 감사 셀** — 회귀 자산이 없는 셀. 에이전트가 증거를 동반해 직접 돈다.
- **자격 시험(필수)**: 어떤 셀을 기계 판정으로 돌리려면 **해당 코드를 고의로 깨뜨려 테스트가 죽는 것을 먼저 증명한다.** 통과만으로는 자격이 없다. 깨뜨렸는데 초록이면 그 셀은 실브라우저 감사 대상이다.
  - 2026-08-17 시행 결과 3/3 사망 확인 — M1 기일경과 후순위(`data.ts`) · M2 `resetAll`(`watchlist.ts`) · M3 오프라인 폴백(`sw.ts`).
- **이 절의 존재 이유**: 회귀가 지키는 셀을 사람이 다시 도는 것이 2차 라운드 비용의 대부분이었다. 중복 감사는 커버리지가 아니라 낭비다.

### 3. 3차 라운드 실브라우저 감사 대상 = 회귀 공백 6영역

테스트 전체에서 **문자열조차 0건**으로 확인된 지점이다. 이 목록 밖을 감사하려면 근거를 남긴다.

`ExitGuard.tsx`(225줄) · `OnboardingSheet.tsx`(218줄) · `PrefsEditor.tsx`(332줄) · `InstallPrompt.tsx`(208줄) · `BottomTabs.tsx`(79줄) · 실데이터 빈 지역(전북 0건 · 강원 2건)

### 4. 단계 순서 (주인님 지정 — 이 순서를 건너뛰지 않는다)

`S1 코드베이스 조사 → S2 오류·개선사항 발굴 → S3 조치 방안 → S4 검증 → S5 머지 마감처리`

- 각 단계 완료 시 **계획 ↔ 결과 1:1 대조(누락 0)** 후에만 다음 단계로 넘어간다.
- 각 단계는 독립 머지 가능해야 한다 — 중단 시 잔여물 0.
- **S4는 공백 6영역의 회귀 테스트 신설을 포함한다.** 이것이 4차 라운드를 불필요하게 만드는 유일한 처방이다.

### 5. Pain Point 등재 형식 (주인님 지정)

`현상 → 재현 경로 → 원인 → 개선안 → 시급성/난이도 → 예상 소요` — 6항 중 하나라도 비면 등재하지 않는다.
심각도 축은 **불안정성**(오류·미작동·상태 유실)과 **불편성**(마찰·혼선·이탈)으로 가른다.

### 6. 영향도 판정 기준 (주인님 지정 — 고객 만족 핵심 2축)

| 축 | 질문 | 미달 시 |
|---|---|---|
| 방문 목적 달성 | 원하는 물건을 **최소 동선**으로 얻는가 | 핵심목표 미달 — 최우선 |
| 지속 방문 동인 | 맞춤 셋업·자기 인식·관심 저장이 **재방문에 이어지는가** | 이탈 요인 — 차순위 |

### 7. 정리정돈 범위 (지시 §7 — 이 범위를 벗어난 파일 생성·삭제 금지)

허용: 찌꺼기 파일 삭제 · 중복 `.md` 통합(단일 기준 문서) · 머지 완료된 stale 원격 브랜치 삭제 · **코드 중복 정리는 "계획서"까지**(실행은 별도 승인).
금지: 위 범위 밖의 임의 생성·삭제. 삭제 대신 비활성화를 먼저 검토한다.

## 5-4. 방문자 감사 백로그 3차 (2026-08-17 — 순번 94~ 고정)

> 감사 실적: 실브라우저 6트랙 · **30/35셀 기입**(나머지 J5-Q1~Q5는 §5-3 2절 기계 판정 셀 — 유닛 141·E2E 22가 지키며 뮤테이션 3/3으로 자격을 증명했다) · 발견 **24건** · 소요 56분 · 6기수.
> 재현 경로는 문장이 아니라 **스펙 파일로 고정한다** — 각 건의 재현은 `tests/e2e/` 회귀 스펙이 소유한다(§5-3 4절 S4 요구). 등급 = 시급성/난이도/예상 소요(분).

| 순번 | 심각도 | 셀 | 현상 | 원인 | 개선안 | 등급 | 상태 |
|---|---|---|---|---|---|---|---|
| 94 | 불안정 | J7-Q1 | 이탈 시트 주 CTA '관심함에 저장'이 무동작 — 시트가 존재하는 이유가 통째로 죽어 있다 | `ExitGuard.tsx:113` save()가 setOpen(false)와 router.push를 같은 틱에 실행 → 무장 effect 재실행이 센티넬을 다시 push해 Next가 router.push를 취소 | 이동 확정 시 재무장을 먼저 차단(bypass 세운 뒤 push) + 라벨대로 실제 관심등록 | 높음/S/25 | S3 수정 |
| 95 | 불안정 | J7-Q5 | 리스트·상세에서 뒤로 1회에 홈이 SPA 복원 없이 문서 전체 재로드(58ms→275ms) | `ExitGuard.tsx:88` pushState가 state를 통째로 교체해 Next `__NA` 마커 소실 → app-router가 reload() 호출 | `{...window.history.state, cvExitGuard:true}`로 마커 보존 | 높음/S/30 | S3 수정 |
| 96 | 불안정 | J7-Q1 | 시트가 열린 채 화면이 바뀌면 착지 화면을 잠근다(관심등록·하단탭 클릭 Timeout) | `ExitGuard.tsx:99` popstate 핸들러에 여는 처리만 있고 닫는 처리가 없다 | pathname이 "/"를 벗어나면 닫는다 | 높음/S/20 | S3 수정 |
| 97 | 불편 | J7-Q2 | 앱 안으로 돌아가는 뒤로가기에도 이탈 경고가 뜬다(오탐) + 홈 방문마다 이력 2칸 누적 | 도착지가 앱 밖인지 보지 않고 센티넬 소진만 판정 | 앱이 만든 엔트리 수를 세어 앱 내 복귀는 통과 | 중간/M/45 | 다음 라운드 |
| 98 | 불편 | J7-Q4 | 라벨 '관심함에 저장'인데 저장 0건 — 라벨·문구·동작 3자 불일치 | `ExitGuard.tsx:113·225·239` | 라벨대로 저장 후 이동(94와 동반) | 중간/S/30 | S3 수정 |
| 99 | 불편 | J7-Q3 | 갱신에서 내려간 id가 남으면 홈은 최근 본 물건 0개인데 시트만 "1건 있다"며 가드 발동 | `ExitGuard.tsx:42` 원본 배열 길이만 셈 vs `RecentViewed.tsx:13` 존재분만 렌더 | 손실 판정을 현재 데이터셋 존재분으로 한정 | 중간/M/35 | 다음 라운드 |
| 100 | 불편 | J7-Q4 | 탭 5종 중 관심함만 h1도 document.title도 없다(/list도 동일) | `watch/page.tsx:244` heading 부재 + client 페이지라 metadata 없음 | h1 신설 + layout.tsx metadata, /list 동반 처리 | 중간/S/25 | 다음 라운드 |
| 101 | 불편 | J7-Q3 | 관심함→상세 진입 시 활성 탭이 관심함이 아니라 홈으로 바뀐다 | `BottomTabs.tsx:46` /item 접두를 출발지 무관 홈 고정 | 직전 섹션 세션 키로 판정 | 낮음/S/30 | 다음 라운드 |
| 102 | 불안정 | J1-Q1 | 저장 막힌 기기에서 온보딩 완료가 기록 안 되는데 시트는 성공한 듯 닫힘 → 매 방문 재노출 | `OnboardingSheet.tsx:50` markOnboarded() 반환값 폐기(바로 위 setPrefs는 검사함) | 저장 경로는 시트 유지·실패 고지, 건너뛰기는 고지 후 세션 플래그로 재노출 차단 | 높음/S/25 | S3 수정 |
| 103 | 불편 | J1-Q5 | 리스트 펼친 상태에서 화면 크기 변경마다 메인 스레드 30~107ms 정지(배너 닫으면 0ms) | `InstallPrompt.tsx:21` measureLift가 main 전수(13,047개) 순회 + resize 직결 무디바운스 | 마커 1개 기반 조회로 축소 + rAF 코얼레스 | 중간/M/60 | 다음 라운드 |
| 104 | 불편 | J2-Q2 | 온보딩에서 0건 지역(전북)을 고르면 첫 화면이 곧바로 "물건 없음" — 칩에 건수 신호 0 | `OnboardingSheet.tsx:174` meta.countsByRegion 미참조(FilterChip.count는 이미 구현됨) | 칩에 건수 병기(0건도 사실대로) | 중간/S/35 | 다음 라운드 |
| 105 | 불안정 | J3-Q1 | 초기화 실패인데 화면이 "0건"으로 바뀐다 — 실패 알림과 0건이 동시 표기 | `PrefsEditor.tsx:260` ok 무관하게 setCounts(0) | 실패 시 저장값을 되읽어 실제 건수 표기(같은 파일 149행 규약) | 중간/S/15 | S3 수정 |
| 106 | 불편 | J6-Q3 | **관심조건을 바꿔도 홈에 영영 반영되지 않는다** — 화면은 "홈 필터에 바로 반영"이라 단언 | `page.tsx:94` URL 분기가 미러를 되써 setPrefs의 무효화를 되돌림 + :97 미러 분기가 :103 applyPrefs보다 앞 | prefs에 변경 시각 추가, 복원 우선순위 URL→prefs(최신)→미러 | 높음/M/45 | S3 수정 |
| 107 | 불편 | J1-Q1 | 최초 방문 홈에서 설치 배너 가시 높이 0px — 보이지도 닫히지도 않는다 | `InstallPrompt.tsx:21` main 안 fixed 요소(온보딩 시트)를 하단 CTA로 오인해 lift 608px | 후보 판정에서 모달 제외 + 시트 닫힘 시 재측정 | 중간/S/25 | 다음 라운드 |
| 108 | 불편 | J7-Q3 | 초기화가 예고 없이 설치 배너 해제 기록까지 지워 배너가 되살아난다 | `watchlist.ts:362` `crazyvalue.` 접두 전량 삭제가 install 키 포함 | 보존 키를 명시 제외(접두 삭제 방식은 유지) | 낮음/S/15 | S3 수정 |
| 109 | 불편 | J4-Q2 | 0건 지역만 고른 방문자는 완화로 결과를 되찾아도 다음 진입에 다시 0건 홈 — 매 방문 같은 복구를 반복 | `list/page.tsx:45` updateFilters가 URL·미러만 갱신하고 setPrefs 미호출 | 0건 화면에 관심조건 수정 동선 제공 또는 완화를 prefs에 반영 | 높음/M/35 | 다음 라운드 |
| 110 | 불편 | J4-Q2 | "법원 선택 후 사건번호를 붙여넣는다"는데 **어느 법원인지 그 블록에 없다**(화면 밖 828px 위) | `item/[id]/page.tsx:160` 안내 문구가 법원명 미포함 | 안내 문구가 법원을 지목(복사 문자열은 사건번호 단독 유지 — 확정 사양) | 높음/S/10 | S3 수정 |
| 111 | 불편 | J4-Q2 | 지역 칩 17개가 231건·2건·0건을 완전히 동일하게 제시 — 17번 탭해야 분포를 안다 | `RegionFilter.tsx:77`·`OnboardingSheet.tsx:173` count prop 미전달(전 호출부 0곳) | FilterChip의 기존 count prop 연결 | 중간/M/40 | 다음 라운드 |
| 112 | 불편 | J4-Q4 | 복사 실패 폴백 문단이 role·aria-live 없이 조용히 삽입 + 2.5초 뒤 라벨만 복귀해 두 상태가 어긋남 | `CopyCaseNo.tsx:54` 폴백에 라이브 리전 없음(백로그 27 처방이 라벨에만 적용) | 폴백에 role 부여 + 라벨·폴백 상태 동기화 | 중간/S/15 | S3 수정 |
| 113 | 불편 | J4-Q4 | 같은 화면의 복사 폴백 둘이 서로 다른 문체("직접 복사한다" vs "직접 복사하세요") | `ShareButton.tsx:60`이 백로그 63 범위에서 누락 | 단정형 통일 + 게이트 문체 룰(117과 동반) | 낮음/S/5 | 다음 라운드 |
| 114 | 불안정 | J4-Q1 | 더보기가 500건에서 무반응 — 버튼은 "500/1000"으로 남는데 눌러도 늘지 않고 피드백 0 | `query.ts:57` Math.min(rawN,500) vs `ItemList.tsx:90` shown<items.length — 상한과 노출 조건이 다른 곳에 있다 | 상한을 한 곳이 소유하고 노출 조건이 그것을 읽게 + 상한 도달 안내 | 중간/S/25 | S3 수정 |
| 115 | 불편 | J4-Q4 | 빈 상태 3곳·404·전역 오류 경계가 습니다체·청유형 — 앱 기준 문체(단정형)와 갈림 | `ItemList.tsx:45·48·49` 등 6파일. **근본 원인은 게이트 공백** — `ui-quality-gate.mjs:91` R12가 문장 수만 세고 문체는 채점하지 않는다(백로그 63의 "게이트 문체 룰 연동"이 문장 수 룰로 대체되며 소실) | 문구 단정형 통일 + **R12에 문체 차단룰 복원** | 중간/M/70 | 일부 S3 수정 |
| 116 | 불편 | J4-Q3 | 전 카드에 "미친가치 픽" 배지가 붙어 변별력 0(1,000건 전량 픽·중앙값 0.24) | `data.ts:125` 절대 임계는 확정 불변식인데 수집이 이미 유찰 2회+만 담아 전량이 조건 충족 | 임계는 그대로 두고 정보량 0인 배지·칩 렌더를 조건부로 | 중간/M/45 | 다음 라운드 |
| 117 | 불편 | J1-Q4 | 홈 주 CTA가 "물건 1,000 건 보기"로 숫자와 단위가 벌어진다(리스트는 "총 1,000건") | `ResultButton.tsx:31` flex 컨테이너가 텍스트 노드를 익명 아이템으로 쪼개고 :37 mx-1이 우측 여백 추가 | 숫자+단위를 한 인라인 요소로 묶는다 | 낮음/S/10 | 다음 라운드 |
| 118 | 불안정 | J4-Q3 | **앱이 틀린 법원을 표시한다** — 대구 물건이 부산지방법원, 대구 달서구 물건이 울산지방법원, 경주·익산 물건이 부산서부지원으로 적혀 있다. 그 법원에서는 사건번호가 조회되지 않고(실측 4/4 "잘못된 번호") 소재지 관할 법원에서는 전부 조회된다 → 원문 도달 불가 + 오정보 | `crawl.ts:666` 법원명 원천이 손코딩 표(`COURT_BY_CODE[boCd].name`)이고 응답 `jiwonNm`은 폴백. region은 주소에서 뽑으므로(`resolveRegionKey`) 법원만 어긋난다 | 원천을 응답 필드로 옮기고 표는 폴백으로 내린다 + 표본 조회 검증(관할 예외·자동차 사용본거지는 정상이므로 지역 불일치만으로 판정하지 않는다) | 높음/M/60 | 미해소(2026-08-20 등재) |

**S3 수정 범위 결정 근거(내 판단)**: 24건 견적 합이 715분이라 이번 창에 전량은 불가능하다. **불안정 전량 + 높음 전량**을 골랐다 — §5-3 6절의 두 축(방문 목적 달성 · 지속 방문 동인)에 직접 걸리는 건들이다.

### 잔여 13건 재판정 (2026-08-17 마감 — 「누가 손해를 보는가」 관문)

> 주인님 지적: *"끊임없이 오류라고, 수정하고, 남고 — 뭔 짓이야."* 지적이 옳다. **개선 여지를 결함과 같은 표에 적은 것이 원인이다.**
> 감사는 돌리면 항상 무언가 찾는다. 기준이 "발견 0"이면 끝이 없다. 그래서 기준을 **"방문자가 실제로 손해를 보는가"** 하나로 바꿔 재판정했다.

| 판정 | 순번 | 근거 |
|---|---|---|
| **닫음 — 손해 없음** | 101 · 107 · 113 · 116 · 117 | 101 활성 탭 표시는 어긋나도 이동은 정상 · 107 배너 미노출은 방문자 손해가 아니라 설치 유도 손해 · 113·117은 문체·자간 · 116 픽 배지는 전량 표시라도 오정보가 아니다. **다시 열지 않는다.** |
| **데이터 뿌리 1개로 통합** | 104 · 109 · 111 | 셋 다 "0건 지역"이 뿌리다. 그런데 전북이 0건인 진짜 이유는 UI가 아니라 **수집 중단**이다(`meta.aborted: true` · 로봇탐지 · `truncated 7918`). 화면에서 우회하는 것은 증상 처리다 — **이슈 #6·#7(주간 full 조기 종료)에 병합**한다. |
| **완료 (2026-08-17 당일 처리)** | 97 · 99 · 100 · 103 · 115 | 주인님 지적("말만 하고 안 한다") 후 같은 날 전부 닫았다. 97 이력 칸 수로 앱 내 복귀 판정 · 99 손실 판정을 산출물 존재분으로 단일화 · 100 `watch`·`list` layout에 h1+metadata(네 갈래 반환 전부 보장) · 103 resize를 rAF로 코얼레스 · 115 문구 9개소 단정형 + **게이트 R13 문체 차단룰 신설**. |

**백로그 잔여 = 1(순번 118).** 순번 94~117 스물넷 중 열여섯을 고쳤고 다섯을 손해 없음으로 닫았으며 셋은 데이터 뿌리(이슈 #6·#7)로 보냈다. 2026-08-20에 118(법원명 오매핑)이 등재됐다 — 감사 라운드가 아니라 원문 링크 결함을 고치던 중 실측으로 확정된 건이다.

### 라운드 종결 조문 (재발을 감사로 잡지 않는다)

1. **4차 감사 라운드를 자동으로 열지 않는다.** 주인님이 명시적으로 지시할 때만 연다.
2. 남긴 5건은 **다음 기능 작업에 곁들여 처리**한다 — 별도 라운드를 만들지 않는다.
3. **재발 방지는 감사가 아니라 게이트가 한다.** 115가 그 증거다 — 2차에서 "문체를 단정형으로 통일"을 처방했는데 게이트가 문장 수 판정으로 바뀌며 문체 룰이 사라졌고, 그래서 3차에 같은 결함이 다시 나왔다. **처방을 게이트에 등재하지 않으면 다음 라운드가 같은 것을 또 찾는다.**
4. 따라서 앞으로 백로그에 올리는 조건은 **"방문자가 손해를 보는가"** 하나다. 개선 아이디어는 백로그가 아니라 §5-5 같은 계획 문서에 적는다.

## 5-5. 코드 중복 정리 계획 (2026-08-17 S1 실측 — 실행은 별도 승인)

> 지시 §7이 요구한 "코드베이스의 중복·불필요 부분 정리 **계획**"이다. 계획까지가 이번 범위이고 실행은 승인 대상이다.
> 순서는 **위험 감소량 ÷ 비용**으로 매겼다. 1번이 이 저장소 최대 부채다 — 미테스트 구간과 정확히 겹친다.

| # | 대상 | 현황 | 처방 | 왜 먼저인가 | 소요 |
|---|---|---|---|---|---|
| 1 | **모달 계약 3벌** — 초기 포커스·Tab 트랩·배경 inert·스크롤 잠금·Escape·클린업 | `ExitGuard`·`OnboardingSheet`·`PrefsEditor`가 각자 구현. `FOCUSABLE` 셀렉터 상수도 3번 선언. **PrefsEditor는 이미 `useDialogContract`로 추출해 뒀는데 나머지 둘이 쓰지 않는다** | 훅을 `src/lib/use-dialog-contract.ts`로 승격하고 셋이 호출 1줄로 대체 | 접근성 계약이 세 곳에서 갈라지면 한 곳만 고치는 절반 처방이 반복된다(백로그 4→70→72가 그 궤적) | 90분 |
| 2 | **알림 이슈 생성 블록 4벌** | `.github/workflows/crawl.yml:193·231·267·368` — 라벨 생성 → 같은 날 제목 조회 → 있으면 종료 → 없으면 생성이 거의 그대로 복제 | composite action 또는 재사용 job으로 1곳 | 2026-08-09 억제 규약 정정이 4곳 동시 수정이었다. 한 곳을 놓치면 **그 알림만 조용히 침묵한다** — 실패를 못 보는 실패다 | 60분 |
| 3 | **산출물 생성 경로 2벌** | `crawl.ts`와 `gen-mock.ts`가 검증 게이트·요약 1줄·17분할 저장·meta 조립을 각각 구현(`gateItems`·`buildSummaryLine`을 gen-mock이 쓰지 않는다) | gen-mock이 `crawl-lib`의 기존 함수를 호출 | 산출 형식이나 Meta 필드가 한쪽만 바뀌면 조용히 갈린다. 이를 막는 것은 `mock-contract.test.ts` 2건뿐이다 | 60분 |
| 4 | **KST 날짜 산출 3계열** | `crawl.ts`(UTC+9 시프트) · `format.ts`(Intl Asia/Seoul) · 게이트(또 다른 +9 시프트). **AGENTS §2 불변식이 "format.ts 단일 함수만 경유"라고 못박았는데 파이프라인이 그 밖에 있다** | 파이프라인을 `format.ts` 경유로, `.mjs` 게이트는 상수 공유 | 지금은 결과가 같아 사고가 안 나지만, 오프셋을 한쪽만 고치면 조용히 갈린다 | 45분 |
| 5 | **문구 상수화** | "이 기기에만 저장" 3곳(문장이 제각각) · 저장 실패 안내 3곳 · **"매일 03:00" 하드코딩 4곳** | `src/lib/copy.ts` 상수 + `SaveFailedAlert` 컴포넌트 | 슬롯을 바꾸는 날 네 곳을 동시에 고치지 않으면 **앱이 방문자에게 거짓을 말한다**(§1은 실발화가 이미 03:53~04:28로 다르다고 적고 있다) | 45분 |
| 6 | **클립보드 폴백 2벌** | `ShareButton`·`CopyCaseNo`가 LABELS·타이머·2초 복귀·AbortError 분기·텍스트 폴백까지 동일(주석이 "같은 규격"이라고 스스로 밝힌다) | `useFlashLabel` 훅 + 폴백 컴포넌트 | 현재 ShareButton만 E2E가 덮는다 — **합치면 기존 테스트가 양쪽을 함께 지킨다**(테스트를 새로 쓰지 않고 커버리지가 는다) | 40분 |
| 7 | **죽은 코드 4건** | `loadMeta`(호출 0) · `FilterChip.count`(호출부 8곳 모두 미전달 → 렌더 분기 도달 불가) · `markAppNavigated`·`Skeleton`의 불필요한 export | 삭제 또는 지역화. **단 `FilterChip.count`는 삭제하지 마라** — 백로그 104·111이 바로 그 prop을 쓰는 처방이다 | 읽는 사람이 쓰이는 코드로 오인한다 | 25분 |
| 8 | **네비게이션 플래그 위치** | `RecentTracker`가 `hasAppNavigated`를 `@/components/ExitGuard`에서 import — 컴포넌트→컴포넌트 역방향 의존 | `src/lib/`로 내린다 | 이탈 가드와 상세 뒤로가기가 한 파일에 결합돼 있다 | 20분 |
| 9 | **localStorage 단일 경유 규약 위반** | `watchlist.ts:4`가 "모든 접근은 이 파일을 경유한다"고 선언하는데 `ExitGuard`·`InstallPrompt`가 직접 접근 | 규약대로 옮기거나 **게이트 차단룰로 등록** | 둘 다 try/catch가 있어 지금은 피해가 없다. 문제는 **규약을 강제하는 게이트가 없어 다음 추가분이 무방비**라는 것이다 | 30분 |

**합계 약 415분.** 1·2번만 해도 위험의 대부분이 사라진다 — 나머지는 언제든 쪼개서 할 수 있다.
**공통 교훈**: 9건 중 5건이 "이미 추출해 둔 것을 쓰지 않는" 형태다(`useDialogContract`·`gateItems`·`buildSummaryLine`·`shiftDays`·`FilterChip.count`). 중복의 원인은 추상의 부재가 아니라 **추상을 만들고 연결하지 않은 것**이다.
