# AGENTS.md — crazyvalue 저장소 작업 규칙

> 작업 재개 순서: 이 파일 → `docs/PLAN.md`(기획안 v3.1 사본, 원본은 기획 폴더 `경매 최저가/`) → `docs/TRACE.md`(실행 완전성 원장) → 마지막 Phase 보고.
> 상위법: `~/.claude/CLAUDE.md` 반복금지 헌법 · 기획 폴더 `경매 최저가/AGENTS.md`.

## 1. 정체성·확정 결정 (재질문 금지)
- **CrazyValue(미친가치)** — 유찰 2회 이상 법원경매 물건만 큐레이션하는 모바일 온리 PWA.
- 스택: Next.js 15 + TypeScript + Tailwind v4 · 정적 JSON(시도 17분할) · GitHub Actions 2슬롯(정기 `0 18 * * 6`=일 03:00 KST + 자동 캐치업 `0 18 * * 0-5`, 데이터 7일 이상 낡았을 때만) · Vercel 자동 배포.
- **갱신 파이프라인에 수동 개입 경로를 두지 않는다(2026-07-27 주인님 지시).** 실패·드롭은 캐치업 슬롯이 스스로 이어받는다. "안 되면 수동 실행"은 해법이 아니다. (단, 파이프라인 코드 수정 직후의 실전 등가 검증 1회는 `workflow_dispatch`로 당일 수행한다 — §9 [2026-07-30].)
- **산출물 = 유효·중복제거 후 최대 1,000건**(`OUTPUT_CAP`, 2026-07-30 주인님 확정). **선별은 임박순 절단이 아니라 다음 갱신일까지의 매각기일 배분**(2026-08-02 주인님 승인 — 임박순 절단은 상한을 첫 기일에서 소진해 갱신 주기의 나머지 날을 전부 기일 경과로 만든다). 기일 내 우선순위는 저가(`priceRatio`) 순. 기준은 /guide에 공개. 게이트는 유효 부분집합 상시 배포, 기각은 유효 0건일 때만.
- 무가입 — 관심함·최근 본 물건은 localStorage `crazyvalue.*`. 코드 위치는 이 저장소(`C:\dev\crazyvalue`)만, 기획 문서는 기획 폴더.
- 워드마크: 미친가치(주)+CrazyValue(보조). "미친"은 브랜드명·픽 배지 한정, 본문 수식어 금지.

## 2. 도메인 불변식
- 저감률은 법원별 20% 또는 30% — 전국 일률 아님. 앱이 임의 계산·단정 표기 금지, 수집값 그대로.
- **미친가치 픽 = `priceRatio <= 0.5`** (최저가 ≤ 감정가 50%) — 저장 필드 아닌 파생 계산, 기준은 앱 안내에 공개.
- `priceRatio` = minPrice/appraisalPrice(0~1). 표시 할인율 = 1 − priceRatio — **부호 반전 주의**.
- 법적 고지(참고용·원문 우선·입찰 전 확인)를 상세·안내에 상시 표기. 투자 권유·수익 보장 표현 금지.
- 크롤 예절: 간격 ≥1초 · UA `CrazyValueBot/0.1` · 심야 실행 · 실패 시 직전 데이터 유지(무커밋).
- 기일 계산은 Asia/Seoul date-only, `src/lib/format.ts` 단일 함수만 경유. 금액은 원 단위 정수 저장, 축약은 표시 단계.

## 2-1. 작업 방식 (2026-07-18 주인님 지시 — 상시 적용)
- **Workflow 병렬 오케스트레이션 의무**: 실행 작업(감사·구현·수정·검증)은 서브에이전트를 최대 활용한 병렬 오케스트레이션(Workflow)으로 수행한다. 오케스트레이션 없이 단독 순차 작업으로 진행하지 않는다(경미한 단일 파일 수정·문서 갱신은 예외).
- **자율 마감**: 작업 단위 완료 시 "마감할까요" 류 재문의를 하지 않는다. 검증(테스트·게이트·E2E) 통과 → 커밋(머지)까지 스스로 마감하고 결과만 보고한다. 사용자 결정이 필요한 지점은 파괴적 변경·범위 변경·외부 퍼블리시 권한뿐.
- **방문자 관점 감사 프로토콜**: 기능 마감 후에는 방문자 Flow(최초 방문→온보딩(맞춤설정)→탐색→관심등록→재방문 맞춤화면→탭·백링크) 전수를 실브라우저로 감사하고, 발견 개선사항을 아래 §5 백로그에 순번으로 등록한 뒤 라운드 단위로 해소한다. 참고: 로그인·로그아웃·회원가입은 §11 범위 제외 확정(무가입 정체성) — 감사에서는 무가입 대체 흐름(온보딩 prefs·localStorage)을 대상으로 한다.

## 2-2. 방문자 감사 헌법 (2026-07-22 주인님 승인 — 라운드 불문 상시 적용)
> 1차 라운드는 트랙 설계를 매 회 새로 짜 커버리지가 라운드마다 흔들렸다. 범위를 매트릭스로 고정해 "빠진 곳"을 기계가 판정하게 한다.

1. **범위 = 35셀 매트릭스가 전부.** 여정 7단계(J1 최초 방문 · J2 관심조건 등록 · J3 맞춤설정(내 설정) · J4 탐색→법원 원문 · J5 관심 추적 · J6 재방문·주간 갱신 반영 · J7 이탈·탭/백링크) × 품질 5축(Q1 불안정성 · Q2 편의성 · Q3 정합성 · Q4 접근성·문구 · Q5 성능). **셀 공란 1개 = 감사 미완, 마감 불가.**
2. **증거 없는 발견은 등재 금지.** 실브라우저 재현 절차와 증거(스냅샷·측정치·콘솔 출력)를 동반한 건만 백로그에 올린다. 코드 읽고 추정한 건은 등재하지 않는다.
3. **적대적 교차검증 통과가 수정 착수 조건.** 등재 건은 독립 에이전트가 반증을 기본값으로 재현 시도한다. 재현 실패·확정 사양·기존 라운드 중복은 폐기하고 사유를 남긴다. 심각도 "불안정"은 3중 검증.
4. **순번 불변.** 1차 라운드(1~30)를 이어 31번부터 부여하고, 이후 라운드에서 재번호를 매기지 않는다.
5. **무가입 정체성 유지 — 인증 도입 금지.** 주인님 지시의 "로그인 등록/로그아웃"은 각각 **관심조건 등록 / 내 데이터 초기화**로 사상해 감사·구현한다.
6. **법원 원문 도달 불변식.** 사건 단위 딥링크는 법원 사이트 구조상 불가하다(`docs/CRAWLER.md` §4.1). 앱은 ① 사건상세 화면 링크 ② 사건번호 복사 ③ 찾는 방법 안내 **3종을 모두** 제공한다. 하나라도 없으면 핵심목표 미달로 판정한다.
7. **감사·수정·검증은 Workflow 병렬 오케스트레이션으로만 수행한다**(§2-1 의무의 감사 라운드 적용형). 변이 구간은 그룹별 파일 소유권을 배타 배정해 병렬화한다.

## 3. 마감 게이트
- 각 Phase = PLAN §8 DoD 기계 검증 + §13 규칙 15 점검 + TRACE.md 행 단위 대조(공란 0) 통과 후에만 커밋·마감.
- UI(.tsx) 변경 시: `node .claude/hooks/ui-quality-gate.mjs --check` → 위반 0건 → `--pass` (Stop 훅이 미검증 마감을 차단).
- **데이터 산출물(`public/data/**`·수집 파이프라인) 변경 시: `node .claude/hooks/data-value-gate.mjs --check` → 위반 0건 → `--pass`.** 채점 기준은 파이프라인 성공이 아니라 **방문자 가치** — R1 기일 커버리지 · R2 갱신 주기 생존 · R3 지역 0건 상한 · R4 이번 주 신규 · R5 신호 정합. 판정 기준 시각은 실행 시각이 아니라 `meta.crawledAt`이라 며칠 뒤 돌려도 결과가 같다. **"런 성공·배포 반영"만 확인하고 마감하지 않는다**(2026-08-02 질책의 근본 처방).
- 커밋 규약: `feat:`/`fix:`/`chore:`/`data:` + Phase당 최소 1커밋. force-push·`--no-verify` 금지. 신규 의존성 = 커밋 메시지에 도입 사유 1줄.
- main은 항상 배포 가능 상태. push = Vercel 자동 배포.
- **세로 리듬(2026-07-19 주인님 확정)**: 모바일 화면 세로폭 낭비 금지 — 섹션 간 16px 기본(`space-y-4`), 설명문 1줄 `leading-snug`, **안내·상태 문구 1문장 압축(2문장 병렬 금지)**, 레이어 간 하단 패딩 중복 금지. 상세 기준·예외(ErrorState 규격·법적 고지·읽기 본문)는 `design-system/MASTER.md` 세로 리듬 오버라이드(게이트 R11·R12 warn 연동).

## 9. 오류 원장 (Error Ledger)
> 형식: `- [날짜] 증상 → 원인 → 처방 1줄`
- [2026-07-17] 비율 필드명 `discountRate`가 표시 개념과 반전 → `priceRatio`로 개명 → 저장 필드는 저장값 의미로 명명.
- [2026-07-17] Lighthouse 12부터 PWA 카테고리 삭제 → PWA 검증은 manifest 200 + SW 등록(Playwright)으로 → DoD 도구는 현행 버전 검증 가능성 확인 후 채택.
- [2026-07-18] create-next-app 기본 브랜치가 master로 생성 → 규칙(§13-8)은 main 전제 → 스캐폴드 직후 `git branch -m master main`으로 정합화.
- [2026-07-18] 게이트 템플릿이 루트 `app/` 구조·diff 기반 스캔이라 src/ 구조에서 미검출 + 커밋 후 --pass 시 빈 스캔 구멍 → src/(app|components) 전수 스캔으로 변경 → 채점기는 "변경분"이 아니라 산출물 전수를 기준으로.
- [2026-07-18] `gh repo create`·`vercel --prod`가 세션 자동 권한 분류기에 차단(외부 공개 액션) → 에이전트 단독 퍼블리시 불가 환경 → 주인님 1회 승인(권한 규칙 추가) 또는 직접 실행으로 해소, 로컬 Phase는 계속 진행하고 배포 검증은 해소 후 일괄 수행.
- [2026-07-18] watchlist 손상 JSON 테스트 실패 → safeRead가 공유 상수(EMPTY_WATCH)를 그대로 반환해 호출부 변이가 상수를 오염 → 기본값은 팩토리로 호출마다 새 객체 생성(공유 가변 기본값 금지).
- [2026-07-18] 내장 브라우저 팬에서 courtauction.go.kr(WebSquare SPA) 본문 렌더 실패(스크린샷 타임아웃·body 0바이트 지속) → 정부 SPA는 팬 환경과 비호환 가능 → 화면 XML 정적 파일 GET으로 enum·필드 기본값을 추출하는 우회를 기본 전략으로.
- [2026-07-18] 게이트 R1이 ErrorState 위임 페이지 3곳을 오탐 → 파일 단위 grep이 컴포넌트 경계를 미인식 → 위임 시 공용 컴포넌트의 role="alert" 보유를 검증하는 판정으로 게이트를 수정(제품 코드 무변경·엄격성 유지 — ErrorState에서 alert 제거 시 다시 차단됨).
- [2026-07-19] WatchCard 배지 E2E에서 getByText 부분 일치가 가시 배지+오버레이 sr-only 요약에 이중 매치(strict 위반) → 같은 문구가 배지·접근 이름에 중복 존재하는 구조 → 배지는 exact 전체 문자열, 접근 계약은 getByRole(link, name 정규식)으로 분리 단언(오버레이 카드형 UI 공통 처방).
- [2026-07-19] 내장 브라우저 팬이 프로덕션 URL 로드 직후 resize·screenshot 30s 타임아웃 반복(렌더러 불응, 2026-07-18 courtauction 선례와 동류) → 팬 재현성 한계 → 실측·스크린샷·측정 자동화는 Playwright MCP를 기본 채널로(E2E와 동일 엔진).
- [2026-07-19] 워크트리 검증에서 turbopack이 node_modules junction을 TurbopackInternalError("filesystem root 밖 심링크")로 거부 + 배경 체인이 `| tail` 뒤 exit 0으로 실패 은폐 → junction 의존성 공유 불가·pipefail 미설정이 원인 → 워크트리 검증은 `npm ci` 실설치, 배경 체인은 `set -o pipefail`, junction 제거는 rm -rf 금지(원본 삭제 위험 — `cmd rmdir`로 링크만 제거).
- [2026-07-19] 작업 트리 E2E 1건 실패(홈 결과 버튼 클릭이 배너 닫기에 가로채임) → 원인은 세로 리듬 변경이 아니라 **미커밋 InstallPrompt 배너(z-30 하단 고정)가 ResultButton(z-30 bottom-14)과 겹침** — HEAD+변경분 워크트리(npm ci) E2E 8/8 통과로 어트리뷰션 증명 → 타 트랙 미커밋분이 섞인 작업 트리에서는 커밋 트리 기준 워크트리 검증으로 분리 판정, 하단 고정 요소 신설 시 기존 고정 CTA와 겹침·z-순서 검사 의무(InstallPrompt 수정은 별도 작업 칩 등록).
- [2026-07-19] 브라우저 실측에서 하이드레이션 직후 클릭+측정을 한 호출에 묶자 언마운트 레이스로 엉뚱한 요소(온보딩 시트 잔상 p)를 측정 → 상태 변경(클릭)과 측정은 별도 호출로 분리하고, 측정 대상은 전역 텍스트 검색이 아니라 컨테이너 스코프 셀렉터(`a[href*="pick=1"] p`)로 지정한다.
- [2026-07-22] "동시 8기수" 계획이 실현 불가 — Workflow 동시 상한은 `min(16, CPU코어−2)`이고 이 장비는 4코어라 **2**다 → 기수 계획을 코어 수로 먼저 나눠 파도 수를 산정하고, 벽시계 추정에 반영한다(총 작업량이 아니라 파도 수가 시간을 결정한다).
- [2026-07-22] 7트랙 병렬 감사에 MCP 브라우저를 쓰려다 세션이 1개뿐이라 서로의 페이지를 덮어쓸 구조였음 → 에이전트별로 **자기 소유 Playwright 스펙 파일을 작성·실행**하는 방식으로 전환 → 병렬 충돌 0 + 헌법 §2-2 조문 2가 요구하는 재현 증거를 테스트 출력으로 확보. 서비스워커는 dev에서 비활성이므로 PWA 감사는 반드시 프로덕션 빌드에서 한다.
- [2026-07-22] 백로그 상태열을 정규식 일괄 치환하다 문서 1행(제목)이 훼손됨 → `.replace`의 앵커가 의도한 표 행 밖에서도 성립할 수 있는데 치환 후 검증을 하지 않았다 → **문서 일괄 치환은 행 단위로 찾아 끝문자열을 검사한 뒤 교체하고, 치환 직후 head/grep으로 훼손 여부를 확인한다.**
- [2026-07-22] 93번(배너·고정 CTA 겹침) 수정 후에도 E2E 1건 실패 — 배너가 **일반 콘텐츠**(리스트 더보기 버튼)를 덮었다. 수정이 `position:fixed` 요소만 회피했기 때문 → fixed 요소는 레이아웃을 밀지 않으므로 **실측 높이를 CSS 변수로 알리고 `main` 하단 여백에 더한다**(`--cv-banner-h`). 하단 고정 요소 신설 시 고정 CTA 겹침과 스크롤 끝 콘텐츠 가림을 **둘 다** 검사한다.
- [2026-07-22] 기획 문서(CRAWLER.md §2.6)가 수집 규모를 "수십 요청 이내"로 적었으나 실측은 **16,302건·약 5.2시간**으로 2~3자릿수 어긋났고, 그 추정 위에 워크플로 상한을 설계했다 → **외부 API 규모는 기획 추정이 아니라 착수 시 1회 실측(dry-run)으로 확정**하고 문서의 추정치를 실측치로 교체한다.
- [2026-07-22] 수집 시간 단축책으로 "매각기일 창 축소"를 채택할 뻔했으나 실측은 42일 16,302건 → 21일 **17,000건**으로 무효였다(매각기일이 근시일 3주에 집중) → 대조군 실험(0일 창 934건)으로 **필터가 작동하는가**와 **그 조작이 효과가 있는가**를 분리 확인한 뒤 채택한다. 그럴듯한 인과(창을 줄이면 건수가 준다)를 측정 없이 믿지 않는다.
- [2026-07-22] 홈 세로폭 절감 중 탐색 에이전트가 홈 컨테이너 `pb-8`을 `main`의 `pb-[calc(6rem+…)]`과 "하단 패딩 중복"이라 지목했으나 제거하지 않음 → ResultButton이 `position:fixed bottom-14`(바닥 ~120px 점유)라 `pb-8`은 그 고정 CTA 클리어런스를 채우는 **기능성 여백**(제거 시 스크롤 끝 카드가 가려짐, 선례 61번과 동류) → "중복처럼 보이는 하단 여백"은 fixed 고정요소 클리어런스인지 실측(고정요소 top Y + 8px) 후에만 제거한다.
- [2026-07-22] (사양 이탈 기록) 홈 첫 화면 기준을 "유찰 2회 이상으로 값이 내려간 물건" **단일 기준으로 통일**(주인님 지시) — 데이터셋 전체가 이미 유찰 2회+라 "미친가치 픽(감정가 50%)" 배너가 경쟁 기준이 되어 첫인상 혼란(제10조 중복/병립) → 홈의 `PickEntry` 제거(`page.tsx`), 감정가 50% 기준은 리스트 카드 픽 배지·`/list?pick`·`/guide`에만 부가정보로 존치 → **PLAN.md §4.3-①·TRACE.md(홈에 픽 진입 포함) 동기화 필요**(다음 편집 시 이 결정으로 갱신, 픽 진입 복원 금지). `src/components/PickEntry.tsx`는 현재 import 0의 고아 파일(빌드 무해) — 후속 정리 대상.
- [2026-07-22] 세로폭 수정 마감 검증 중 프로덕션 `/`에서 React #418(하이드레이션 불일치)이 간헐 관측 → **로컬 급속 재빌드 중 이전 빌드의 서비스워커(serwist)가 옛 청크를 캐시로 물고 새 프리렌더 HTML과 섞인 테스트 아티팩트**였다(dev엔 SW 없어 0·prod만 발생, 같은 화면의 prod 프리렌더 HTML == dev 요청시 HTML 바이트 동일 → 실제 렌더 불일치원 없음). `navigator.serviceWorker` 등록해제 + `caches` 전삭제 + localStorage clear 후 프레시 리로드 시 0. → **PWA(SW) 앱을 로컬에서 여러 번 재빌드하며 같은 포트·같은 브라우저로 검증할 때 하이드레이션/RSC 404 에러가 나오면 코드부터 의심하지 말고 SW·caches부터 비우고 재확인**한다. 실배포는 배포별 일관 + serwist precache 버저닝으로 무해.
- [2026-07-27] 주간 갱신이 한 번도 성사되지 않음(프로덕션은 여전히 목데이터) → 원인 3중: ① 스케줄 기회가 2회뿐이었고 07-19분은 리포지토리 생성(07-18 20:01Z)이 cron 시각(07-18 18:00Z)보다 늦어 미등록, ② 07-26분은 실행됐으나 43분·약 2,160요청 지점에서 `ipcheck=false` 로봇탐지 — 계획 총량 9,307요청(2.9시간)의 25% 지점이라 **현 설계로는 완주 자체가 불가능**, ③ 어떤 실패든 쓰기 0·exit 1인 all-or-nothing 계약이라 확보한 18,884건이 전량 폐기 → 처방: 상세 요청을 차단 임계(실측 약 2,300회) 아래로 감축 + 차단을 조기종료로 강등해 부분 산출·커밋 + 요일 분할 누적. **"실패 시 직전 데이터 유지"는 직전 데이터가 실데이터일 때만 안전망이다 — 초기 목데이터 상태에서는 영구 정지 장치가 된다.**
- [2026-07-27] 위 실패의 요청 수를 로그가 아니라 경과시간÷1.2초로 역산해야 했음 → `liveRequestCount`가 dry-run 분기에서만 출력됨(crawl.ts:776) → **요청 카운터·차단 시점은 실행 모드와 무관하게 항상 로깅**한다(차단 임계는 사후 재현이 불가능한 1회성 관측치다).
- [2026-07-27] `BUDGET.maxRequests`가 착수 전 1회 계산치라 상한이 지켜지지 않음 → `request()`는 재시도마다 카운터를 올리는데 목록·상세 루프가 예산을 재대조하지 않았다(상세 1,026건 × 재시도 4회 = 상한의 3배) → **상한은 계획치가 아니라 루프 선두의 러닝 체크로 강제**한다(목록 소진=전체 실패, 상세 소진=조기 종료).
- [2026-07-27] HTTP 429를 5xx와 같이 재시도 대상으로 분류 → 서버가 감속을 요구한 순간 물건당 요청이 4배로 늘어 차단을 가속 → **레이트리밋은 Retry-After를 존중해 길게 1회만 쉬고 반복되면 중단 신호로 승격**한다(CRAWLER.md §4 "429/550 시 지수백오프·중단").
- [2026-07-27] 최저가 결측 시 목록가(`minmaePrice`, 실측상 직전 회차 가격) 폴백이 `minPrice==감정가`를 만들어 priceRatio 1.0·저감률 0 이력이 zod를 통과 → superRefine은 저장값끼리의 정합만 보므로 원리적으로 검출 불가 → **도메인 불가능값은 스키마가 아니라 매핑 단계 스킵 규칙으로 막는다**(유찰 2회 이상인데 최저가≥감정가면 제외).
- [2026-07-27] `calibrateFailedCode`가 관측 1회짜리 코드로 유찰 결과코드를 확정 → `count > bestCount` 비교에 표본 하한·마진이 없어 조기 종료로 표본이 얇아지면 유찰/변경 라벨이 뒤집힌 채 게이트 통과 → **런타임 자가 보정에는 표본 하한(200건)과 2위 대비 우세 배수(3배)를 함께 강제**하고 채택 근거를 로깅한다.
- [2026-07-27] `partial` 한 필드에 "차단됐다"와 "후보가 예산보다 많다"를 겹쳐 담아 상시 참이 됨(신호 소실) + 조기 종료가 exit 0이라 `if: failure()` 알림이 발화하지 않아 전량 역산 이력이 조용히 배포될 경로 → **운영 신호는 의미 단위로 분리**(aborted·abortReason·abortedAtRequest·totalRequests·truncated·historySource·scope)하고 워크플로가 meta로 조기 종료를 판정해 이슈를 만든다.
- [2026-07-27] `needsDetail`(픽 후보 판정)이 `minmaePrice`를 쓰는데 실제 `minPrice`는 `notifyMinmaePrice1`에서 온다 → 판정 가격과 산출 가격이 **다른 필드**라 비율이 한 회차분 과대평가되고 실제 픽이 상세 후보에서 탈락(dry-run 표본 5건 전부 후보 0 — 실제 0.80을 1.00으로 판정) → **같은 값을 판정과 산출에서 두 번 유도하지 말고 폴백 체인을 공유**한다(상세 의존 항만 제외).
- [2026-07-27] `report failure`의 `if: failure() && steps.gate.outputs.run == 'true'`는 게이트 스텝 자신이 실패하면 outputs가 빈 문자열이라 거짓이 돼 **유일한 실패 알림이 침묵** → 조건을 `!= 'false'`로 뒤집어 미판정도 알림 대상에 넣는다. **가드 조건은 fail-safe 방향(모르면 알린다)으로 쓴다** — 알림 경로에서 fail-open은 침묵 실패다.
- [2026-07-27] 커밋 제목에 `@` 문자가 새어 들어가 규약(`fix:` 접두)이 깨짐(bf2a778) → Bash 도구에 PowerShell here-string(`@'…'@`)을 써서 리터럴 `@`가 메시지 첫/끝 줄로 들어갔다 → **셸이 다르면 다중행 문자열 문법도 다르다** — Bash는 heredoc, PowerShell은 here-string. 본문은 온전하고 force-push 금지(§3)라 이력은 정정하지 않는다.
- [2026-07-27] **(주인님 지시 2회 = 하네스 실패)** "자동 설정"을 지시받고도 게이트에 날짜 `2026-07-28`을 하드코딩하고 "드롭되면 수동 실행하세요"를 운영 절차로 남겼다 → 1회성 조건은 그 한 번을 놓치면 영구 미복구다(GitHub은 고부하 시 스케줄을 지연이 아니라 **드롭**한다 — 런도 이슈도 흔적이 없다) → **자동화의 판정 기준은 "날짜·횟수"가 아니라 "목표 상태"로 쓴다.** 캐치업 슬롯 `0 18 * * 0-5`가 데이터 나이 ≥7일일 때만 매일 03:00 KST에 스스로 재시도한다(성공하면 나이 0이라 조용해진다). **복구 경로에 사람이 남아 있으면 자동화는 미완이다** — 마감 전 "이 설계에서 사람이 손대야 하는 지점이 하나라도 있는가"를 자문한다.
- [2026-07-27] (기각 기록) 검토자가 `notifyMinmaePrice1`을 미검증 컬럼으로 단정했으나 dry-run 실측이 반증 — 상세 0건 실행에서 최저가가 감정가의 80%로 산출된 것은 이 컬럼이 채워졌다는 증거다 → **폴백 체인의 어느 항이 발화했는지는 산출 수치에서 역추적할 수 있다**(코드만 읽고 "미검증"으로 단정하지 않는다).
- [2026-07-30] **(주인님 질책 = 하네스 실패)** 파이프라인 수정의 검증을 야간 슬롯(하루 1회)에 위탁해 5일간 실패 모드를 하루 1개씩만 소진(07-25 차단 → 07-28 중복 기각 → 07-29 fetch failed) → **파이프라인 수정은 당일 `workflow_dispatch` 1회로 실전 등가 검증까지 마감**한다. 야간 슬롯은 검증 수단이 아니라 정상 운영이다.
- [2026-07-30] 게이트 철학 전환 — "오류 0건이어야 배포"가 예상 가능한 환경 이상(서버 재서빙 중복)을 전량 실패로 증폭(07-28 고유 약 9,400건 확보하고도 폐기) → **게이트는 유효 부분집합 상시 배포로 설계하고 이상은 개별 드롭+카운트 로깅, 기각은 유효 0건일 때만.** 07-27 "부분 산출·커밋" 처방을 게이트 자신에게까지 적용한 완결편.
- [2026-07-30] 재서빙 중복은 야간 배치 창 전유물이 아니다 — 주간 dry-run 첫 페이지에서도 40행 중 16행 중복 실측 → **수집량 계산·페이지네이션 종결 판정은 항상 고유 건수 기준**으로(중복 포함 누적치는 언제나 오염돼 있다).
- [2026-07-30] 03:40 KST에 당일 캐치업 미발화를 "드롭 재발"로 단정했으나 선례상 발화는 53~83분 지연(18:53~19:23Z)이 정상 범위 → **드롭/지연 판별은 슬롯 시각+2시간 이후에만 확정**한다(그 전 판정은 추측이다).
- [2026-07-30] 대기열 런의 checkout은 시작 시각이 아니라 **이벤트 생성 시점의 SHA로 고정** — concurrency 대기 후 시작한 캐치업이 직전 런의 데이터 커밋(25분 전 이벤트 이후 푸시)을 못 보고 옛 meta로 run=true 이중 실행, 오탐 실패 이슈 #5 생성 → 게이트 판정 직전 `git pull --ff-only`로 최신 main 정렬. 대기열을 거친 런의 워킹트리는 항상 낡았다고 가정한다.
- [2026-07-30] 로봇탐지 임계 실측 2점 — 약 2,160회(07-26)·1,123회(07-30): 임계는 상수가 아니라 변동한다 → 방어는 예산 하향(회피 보장 불가)이 아니라 **강등**(조기 종료+부분 배포)이 정답. 세션 GET transport 거부는 로봇탐지 중단 1분 뒤 재현(누적 표본 2) — 상관은 가설로만 기록, 3회째 관측 시 실행 시각 분산 검토.
- [2026-07-23] 휴대폰 홈에서 하단 고정 CTA·탭바가 오른쪽으로 밀려 잘림(주인님 "프레임이 개판") → 원인은 **ItemCard 내부 `sr-only`(position:absolute)에 기준 블록이 없어 가로 캐러셀(RecentViewed·NewThisWeek의 `overflow-x-auto`) 밖으로 탈출**, 문서 `scrollWidth`를 631(뷰포트 397)로 늘려 모바일 레이아웃 뷰포트가 확장된 것(고정요소는 넓어진 레이아웃 뷰포트 기준으로 배치돼 밀림) → **ItemCard 루트에 `relative` 추가**로 기준 블록 복원(scrollWidth 412 = 뷰포트, 고정요소 l0/r412). **규칙: 가로 스크롤 컨테이너 안에 절대배치 자손(`sr-only` 포함)을 두면 반드시 조상에 `relative`를 준다.** 또 이 증상은 **"최근 본 물건"이 있는 상태에서만** 재현돼, 빈 상태만 캡쳐한 앞선 감사가 놓쳤다 → 회귀 가드 `tests/e2e/layout-overflow.spec.ts` 추가. **UI 감사는 빈 상태만 보지 말고 데이터가 채워진 상태를 반드시 포함한다.**
- [2026-08-02] 08-01 정기 슬롯은 성공·배포됐으나 산출 1,000건 **전량이 매각기일 08-03 하루**(실측: distinctDates 1) → `OUTPUT_CAP`(1,000)이 월요일 하루 물량보다 작아, 유효 9,359건을 임박순으로 자르면 첫날에서 소진된다(직전 주는 목요일 캐치업이라 07-30·07-31·08-03 3일로 우연히 분산됐을 뿐 같은 결함) → 갱신 주기 7일 대비 산출물 유효수명 1일: **08-04(화)부터 전 물건이 "기일 경과"**(앱은 경과분을 후순위 정렬만 하고 숨기지 않는다 — 백로그 12) + 그날 기일이 없는 서울·대구·광주·울산·제주가 통째로 0건. **상한은 "건수"가 아니라 "다음 갱신일까지의 기일 커버리지"로 잡는다**(예: 기일별 균등 배분 또는 창 전체 표본). 조기 종료(이슈 #6)는 이력 정밀도 문제일 뿐 이 결함의 원인이 아니다 — 상한만으로 재현된다.
- [2026-08-02] **(주인님 질책 = 하네스 실패)** 갱신 점검을 "런 성공 → 배포 반영 → 정상"까지만 확인하고 마감하려 했다 → **검증이 파이프라인 안쪽에서 멈추고 방문자 화면까지 오지 않았다** — 산출 1,000건이 하루로 수렴하고 시도 5곳이 0건이고 "이번 주 신규"가 0건인데도 게이트·런·배포가 전부 성공이었다(성공 신호가 실패를 덮는다) → **점검 순서를 뒤집는다: 방문자 화면(홈 첫 진입 상태)을 먼저 보고 그 다음에 파이프라인을 본다.** 기계 처방은 `data-value-gate.mjs`(§3) — 파이프라인 성공이 아니라 방문자 가치를 채점하고 미달이면 마감을 차단한다. **"성공했다"는 신호를 늘리는 대신 "쓸모없다"를 잡는 채점기를 늘린다.**
- [2026-08-02] 갱신이 됐는데 홈에 그 사실이 드러나지 않았다(주인님: "홈화면에서도 표현이 되어야 방문자가 인식한다") → 표기가 `데이터 기준: …`이라는 수동형 회색 12px 한 줄뿐이었고, 갱신을 체감시킬 "이번 주 신규" 섹션은 0건이라 사라졌다 → `isFreshUpdate`(수집일 3일 이내)로 **"새로 갱신" 배지**를 기준일 앞에 붙이고(값 중복 없이 수식만), 신규 0건은 게이트 R4로 차단한다. **갱신은 "일어났는가"가 아니라 "화면에서 보이는가"로 판정한다.**
- [2026-08-02] git 로그 시각을 `--date=format-local:'… UTC'`로 뽑아 KST 값에 UTC 라벨을 붙일 뻔함(cron 슬롯 판정이 9시간 어긋난다) → `format-local`은 로컬(KST) 포맷이다 → **시각 라벨은 포맷 문자열에 손으로 쓰지 말고 `--date=iso-strict`(오프셋 동반)로 출력**한다.

## 5. 방문자 감사 백로그 (2026-07-18 1차 — 순번 고정)
> 근거: 방문자 Flow 병렬 감사 5영역(온보딩·필터/리스트·상세 액션·관심함 재방문·탭/백링크) 발견 32건 → 중복 통합·그룹 분할 후 30건. 순번은 이후 라운드에서 불변. 확정 사양(무가입·주1회 갱신·사진 플레이스홀더·목데이터)은 결함 아님. Lighthouse P점수(TBT) 개선은 기지(旣知) defer 항목.

| 순번 | 심각도 | 제목 | 수정 방침 | 그룹 | 상태 |
|---|---|---|---|---|---|
| 1 | 불안정 | localStorage 형태 손상 시 관심함·홈·상세 전면 크래시(복구 없이 영구 반복) | watchlist.ts safeRead에 형태 가드(items 순수 객체·snapshot 3필드·ids 배열 검증, 불일치 시 기본값 치환 저장) + vitest 손상 케이스 3종 | watch | 완료(2026-07-19 검증 그린) |
| 2 | 불안정 | 클라이언트 예외 시 영문 전면 에러로 앱 셸 소멸(error.tsx 부재) | src/app/error.tsx 신설 — 앱 셸 유지 한국어 오류 화면 + 다시 시도 동선 | nav | 완료(2026-07-19 검증 그린) |
| 3 | 불안정 | 홈 필터 useState 휘발 — 리스트·상세 뒤로가기 복귀 시 선택·건수 전부 초기화(감사 3건 중복 보고) | 홈 필터를 sessionStorage(+URL 쿼리)로 승격, parseListQuery/buildListQuery 재사용해 재마운트 복원 | home | 완료(2026-07-19 검증 그린) |
| 4 | 불안정 | 온보딩 모달 포커스 트랩·배경 inert·Escape 전무 — 키보드 배경 이탈 시 선택 유실·미완료 잔존 | 열림 시 내부 포커스 이동 + 배경 inert + Escape=건너뛰기 동작(키보드 이탈 결함 동일 수정으로 해소) | onboarding | 완료(2026-07-19 검증 그린) |
| 5 | 접근성 | 필터 조작 시 결과 건수 갱신이 스크린리더에 무공지 | ResultButton 건수 텍스트를 aria-live=polite 영역으로 | home | 완료(2026-07-19 검증 그린) |
| 6 | 접근성 | 시도 선택 시 시군구 칩 등장이 스크린리더에 무공지 | RegionFilter 시군구 컨테이너에 role=status·등장 안내 텍스트 | core-lib | 완료(2026-07-19 검증 그린) |
| 7 | 접근성 | 정렬 select 13px — iOS 포커스 시 강제 화면 확대 | ItemList select 글꼴 16px 이상 적용 | list | 완료(2026-07-19 검증 그린) |
| 8 | 접근성 | 카드 감정가 취소선 대비 3.17:1(전 카드 공통, 기준 4.5:1) | ItemCard 취소선 text-ink/50→/70 상향(list·watch 공용 카드) | list | 완료(2026-07-19 검증 그린) |
| 9 | 접근성 | 상세 ink/60 텍스트 3곳 대비 4.19~4.25:1(사건번호 줄 포함) | 상세·PriceStructure의 text-ink/60→/70 상향 | detail | 완료(2026-07-19 검증 그린) |
| 10 | 접근성 | 관심함 기기 저장 고지 대비 4.19:1 | watch/page.tsx 고지 text-ink/60→/70 상향 | watch | 완료(2026-07-19 검증 그린) |
| 11 | 접근성 | 상태 배지가 카드 링크 접근 이름에 미포함·제거 버튼 접근 이름 전부 동일 | WatchCard 링크 내 sr-only 상태 텍스트 + 제거 버튼 aria-label에 물건 식별 병기 | watch | 완료(2026-07-19 검증 그린) |
| 12 | 편의성 | 기본 정렬(기일 임박순) 최상단이 입찰 불가 기일 경과 7건 | sortItems date 분기에서 dday<0 후순위 배치 + 단위 테스트 | core-lib | 완료(2026-07-19 검증 그린) |
| 13 | 편의성 | 만료·오류 URL이 영문 기본 404 — 공유 유입 막다른 길(감사 2건 중복 보고) | src/app/not-found.tsx 신설 — 한국어 안내(갱신 소멸 가능성 1줄) + 홈·리스트 CTA | nav | 완료(2026-07-19 검증 그린) |
| 14 | 편의성 | 동명 시군구 칩 병합으로 선택 범위 암묵 확장(대구 북구→광주 북구 자동 포함) | 시군구를 시도 결합 키로 저장·필터·표기(칩에 시도 병기, d 파라미터 인코딩 변경) | core-lib | 완료(2026-07-19 검증 그린) |
| 15 | 편의성 | 리스트에 적용 중 필터 표시 없음(픽 배너만 존재) | parseListQuery 결과를 상단 요약 칩으로 렌더(개별 해제 시 URL 갱신) | list | 완료(2026-07-19 검증 그린) |
| 16 | 편의성 | /list·/item에서 하단 탭 활성 표시 전무 | BottomTabs 활성 판정에 /list·/item→홈 탭 매핑 | nav | 완료(2026-07-19 검증 그린) |
| 17 | 편의성 | 관심함 변화 배지가 렌더 1회로 소멸(상세 왕복만으로 유실) | 판정 diff를 sessionStorage 세션 캐시로 유지 + 배지에 이전→현재 최저가 병기 | watch | 완료(2026-07-19 검증 그린) |
| 18 | 편의성 | 설치형(standalone) 상세 화면에 가시적 뒤로가기 부재 | 상세 상단 뒤로가기 버튼(history 없으면 /list 폴백) | detail | 완료(2026-07-19 검증 그린) |
| 19 | 편의성 | 온보딩 이후 관심조건 변경·재설정 경로 전무 | /guide에 관심조건 편집 섹션(setPrefs 재사용, 저장 즉시 반영) | nav | 완료(2026-07-19 검증 그린) |
| 20 | 편의성 | 유찰 타임라인이 이번 회차·현재 최저가로 미연결 | HistoryTimeline 말미에 saleDate·minPrice 파생 "이번 회차(예정)" 행 추가 | detail | 완료(2026-07-19 검증 그린) |
| 21 | 편의성 | 매각 종료 카드에 물건 식별 정보 부재(카드 간 구별 불가) | 1차: id 파싱해 지역·사건번호 표기, 2차: snapshot에 address·category 확장 | watch | 완료(2026-07-19 검증 그린) |
| 22 | 편의성 | 기일 경과 카드 제거 수단 부재 + 기일 경과 문구 중복 표기 | expired 카드에 제거 버튼 노출(종료 카드와 대칭) + 문구 1회만 표기 | watch | 완료(2026-07-19 검증 그린) |
| 23 | 편의성 | 관심조건 선택 후 건너뛰기 탭 시 무경고 폐기(복구 불가) | 선택 1개 이상이면 폐기 확인 1줄 표시 후 진행 | onboarding | 완료(2026-07-19 검증 그린) |
| 24 | 편의성 | 온보딩 열림 중 배경 스크롤 미잠금(스크롤 체이닝) | open 동안 body 스크롤 잠금 + 시트 overscroll-contain | onboarding | 완료(2026-07-19 검증 그린) |
| 25 | 편의성 | 빈 결과 완화 버튼(금액 범위 넓히기)이 조건 따라 완전 무동작 | 완화 버튼 조건부 노출(해당 필터 존재 시), 둘 다 없으면 용도 해제 제안 | list | 완료(2026-07-19 검증 그린) |
| 26 | 편의성 | 인근 지역 포함 라벨과 실동작(지역 전체 해제=전국) 불일치 | 라벨을 "지역 조건 해제(전국 보기)"로 정정 | list | 완료(2026-07-19 검증 그린) |
| 27 | 편의성 | 클립보드 차단 환경에서 공유 실패 시 무피드백 | ShareButton catch 분기 — AbortError만 무시, 실패 라벨 aria-live 표시 + URL 텍스트 폴백 | detail | 완료(2026-07-19 검증 그린) |
| 28 | 성능 | 재방문 저속망에서 온보딩 시트 약 233ms 플래시(beforeInteractive 실행 미보장) | 외부 onboarding-flag.js 폐기, layout.tsx head 인라인 스크립트로 페인트 전 실행 보장 | onboarding | 완료(2026-07-19 검증 그린) |
| 29 | 성능 | 더보기·정렬 탭마다 서버 RSC 왕복(672~1243ms) 후 화면 반영 | n·sort 변경을 셸로우 URL 갱신(history.replaceState)+로컬 상태로 처리해 왕복 제거 | list | 완료(2026-07-19 검증 그린) |
| 30 | 성능 | 관심함 진입 시 관심 건수 무관 전 지역 18파일 로드 | watchlist id 지역 접두 파싱해 필요 파일만 로드 | defer | 보류(현 규모 체감 결함 아님 — Phase 4 서비스워커 /data 캐싱과 함께 재평가) |

- 미실시 감사 2영역(복원력·문구/접근성 전수) → §5-2 2차 라운드에서 35셀 매트릭스로 흡수·해소.

## 5-2. 방문자 감사 백로그 2차 (2026-07-22 — 순번 31~ 고정)
> 범위·판정은 §2-2 헌법. 착수 전 확정된 기지(旣知) 결함 4건(A 목데이터 법원 링크 루트 고정 · B `scripts/crawl.ts` 미커밋으로 주간 갱신 파이프라인 파손 · C 갱신 지연 미표기 · D Phase 4 미커밋·설치 배너 겹침)은 감사 결과와 무관하게 수정 대상이다.
> 승인 신설 사양 5건: N1 내 설정(`/me`) · N2 이탈 확인 팝업 · N3 법원 링크 3종 · N4 주간 갱신 복구 · N5 Phase 4 마감.

> 감사 실적(2026-07-22): 7트랙 × 5축 **35/35셀 기입** · 원발견 62건 → 적대적 검증 **확정 58 · 폐기 4** · 완결성 비평가 추가 8건 = **66건** → 교차 트랙 중복 통합 후 **순번 31~93(63건)**. 12기수·소요 128분.
> 검증 스펙은 `tests/e2e/audit-*.spec.ts`(마감 시 삭제). 폐기 4건은 재현 실패·확정 사양 오인으로 등재하지 않는다.

| 순번 | 심각도 | 셀 | 제목 | 수정 방침 | 그룹 | 상태 |
|---|---|---|---|---|---|---|
| 31 | 불안정 | J5-Q1 | 저장 실패(용량 초과·프라이빗)에도 성공 상태 표시 — 관심등록·온보딩·관심조건 3곳 공통, 실제 저장 0 | watchlist 쓰기 함수가 성공 여부를 반환하고 호출부 3곳이 실패를 표시 | me-core | 완료 |
| 32 | 정합성 | J2-Q3 | 홈 세션 미러가 prefs보다 우선 — 홈 필터를 한 번이라도 만진 세션에서 관심조건 변경이 영구 미반영(안내는 반영된다고 표기) | 복원 우선순위를 URL→prefs 변경시각→세션으로 교정, prefs 저장 시 세션 미러 무효화 | me-core | 완료 |
| 33 | 정합성 | J3-Q1 | 두 탭 동시 편집 시 나중 저장이 앞 저장을 통째 덮어씀 + 앞 화면은 옛 상태 유지 | storage 이벤트 구독으로 타 탭 변경 반영, prefs 필드 단위 병합 | me-core | 완료 |
| 34 | 정합성 | J3-Q1 | 관심조건 칩 1회 탭이 onboarded=true를 세워 온보딩을 영구 미노출 | 온보딩 완료 플래그는 온보딩 시트에서만 기록 | me-screen | 완료 |
| 35 | 정합성 | J5-Q1 | 형태 가드가 saleDate를 typeof string으로만 검사 — 손상 값 렌더·화면에 `(undefined)` 노출 | 날짜 형식(YYYY-MM-DD) 검증 추가, 불일치 시 항목 폐기 + 회귀 테스트 | me-core | 완료 |
| 36 | 정합성 | J4-Q3 | 원천 JSON 손상 시 검증 없이 거짓 사실 단정 표기(픽 배지·할인율·금액) | 클라이언트 로드 시 zod 파싱 실패 항목 제외 + 제외 건수 고지 | data | 완료 |
| 37 | 정합성 | J4-Q3 | 존재하지 않는 매각기일이 조용히 실제 날짜로 이월돼 유효한 D-day로 표기 | format.ts 날짜 파싱을 엄격 검증(roll-over 금지)으로 교정 | data | 완료 |
| 38 | 정합성 | J6-Q3 | 기준일 바가 수집 시각을 리터럴 `03:00`으로 하드코딩 — crawledAt 실시각과 무관하게 항상 03:00 | crawledAt의 실제 시각을 포맷해 표기 | data | 완료 |
| 39 | 정합성 | J1-Q1 | 데이터 로드 실패 후 복구해도 기준일 바 오류가 세션 내내 잔존(전체 새로고침에서만 회복) | useMeta에 재시도 경로 연결 — 본문 "다시 시도"가 meta도 재요청 | data | 완료 |
| 40 | 정합성 | J1-Q3 | "이번 주 신규"가 10일 지난 주간을 "이번 주"로 표기 — 13~16일 전 유찰 물건을 최신으로 오인시킴 | 판정창을 오늘 기준으로 산정하고, 갱신 지연 시 라벨을 기준일 병기로 전환 | data | 완료 |
| 41 | 정합성 | J6-Q3 | 열린 세션에서 데이터·기준일을 한 번도 재검증하지 않아 설치형 PWA가 무기한 낡은 화면 유지 | visibilitychange 복귀 시 meta 재검증 + 변경 시 갱신 안내 | data | 완료 |
| 42 | 정합성 | J6-Q3 | 주간 크롤 워크플로에 실패 알림·동시성 가드·타임아웃 전무 — 실패해도 아무도 모름 | crawl.yml에 concurrency·timeout-minutes·실패 시 이슈 생성 추가 | data | 완료 |
| 43 | 정합성 | J6-Q5 | 서비스워커가 갱신 직후 첫 재방문에 지난주 데이터·기준일을 최신으로 제공 | /data 요청을 NetworkFirst(짧은 타임아웃)로 전환하고 meta는 캐시 우회 | pwa | 완료 |
| 44 | 정합성 | J4-Q1 | parseListQuery가 지역(r)을 화이트리스트 검증하지 않고 4축 중복도 미제거 — 유령 칩·중복 칩 렌더 | r 화이트리스트 검증 + 4축 dedupe + 단위 테스트 | list-nav | 완료 |
| 45 | 정합성 | J4-Q3 | **핵심목표 미달** — 법원 원문 도달 3종 중 ②사건번호 복사·③찾는 방법 안내가 앱 어디에도 없음 | 상세에 사건번호 복사 버튼 + 찾는 방법 1줄 신설(N3) | detail-link | 완료 |
| 46 | 정합성 | J4-Q3 | 기일 경과 물건 타임라인 말미가 "이번 회차(예정)" — 같은 화면 "남은 기간: 기일 경과"와 모순 | dday<0이면 예정 행 미표기 또는 "기일 경과"로 라벨 전환 | detail-link | 완료 |
| 47 | 편의성 | J1-Q2 | 온보딩 시트 주 CTA 2개가 첫 페인트 가시 높이 0px(162px 스크롤해야 등장) — 최초 방문에 진행 수단 없음 | 시트 레이아웃을 헤더/스크롤 본문/고정 푸터로 분리해 CTA 상시 노출 | onboarding | 완료 |
| 48 | 편의성 | J2-Q2 | 건너뛴 방문자에게 나중 설정 가능 신호 부재 + 선택 개수 피드백이 폐기 확인문에만 등장 | 시트에 재설정 위치 1줄 + 선택 개수 상시 표시 | onboarding | 완료 |
| 49 | 편의성 | J2-Q4 | 시트 안내 3문단이 전부 2문장 병렬(세로 리듬 위반)인데 게이트 R12가 습니다체만 검사해 단정형을 구조적으로 미검출 | 문단 1문장 압축 + 게이트 R12를 문체 불문 문장 수 판정으로 교정 | onboarding | 완료 |
| 50 | 편의성 | J1-Q2 | 온보딩 건너뛴 첫 화면에 앱 목적 문구가 11px 태그라인뿐 — 용어 설명은 화면 밖 | 홈 상단에 1줄 목적 문구(기존 태그라인 승격, 중복 신설 금지) | a11y-global | 완료 |
| 51 | 편의성 | J3-Q2 | 하단 탭·페이지 제목 어디에도 "내 설정" 단서가 없어 관심조건 편집 화면을 찾을 수 없음 | `/me` 신설 + 하단 탭 4번째 "내 설정"(N1) | me-screen | 완료 |
| 52 | 편의성 | J3-Q2 | 자기인식 지표(관심함 건수·최근 본 건수·현재 적용 조건 요약) 전무 | `/me`에 저장 현황 블록(N1) | me-screen | 완료 |
| 53 | 편의성 | J3-Q2 | 내 데이터 초기화(무가입 앱의 로그아웃 대응) 수단이 앱 전체에 없음 | `watchlist.resetAll()` + `/me` 초기화(확인 경유)(N1) | me-screen | 완료 |
| 54 | 편의성 | J4-Q1 | 리스트 "더보기" 더블탭 시 두 번째 탭이 새로 삽입된 카드에 적중해 엉뚱한 상세로 이탈 | 더보기 후 삽입 영역 클릭 차단(리스트 앵커 유지) 또는 버튼 위치 고정 | list-nav | 완료 |
| 55 | 편의성 | J4-Q2 | 리스트에서 해제한 지역이 뒤로가기 홈 복귀 시 되살아나 홈·리스트가 서로 다른 건수를 주장 | 리스트의 필터 변경을 홈 세션 미러에 동기화 | list-nav | 완료 |
| 56 | 편의성 | J4-Q2 | pick=1 전용 모드를 리스트에서 해제할 수단이 없고 "적용 중 필터"에도 미표시 | 픽을 해제 가능한 요약 칩으로 편입 | list-nav | 완료 |
| 57 | 편의성 | J4-Q2 | 더보기로 펼친 목록이 정렬 변경만으로 10건으로 되접힘 | 정렬 변경 시 n 유지 | list-nav | 완료 |
| 58 | 편의성 | J5-Q2 | 기일이 남은 정상 관심 카드에 제거 버튼이 없어 상세 왕복이 강제됨 | 전 카드에 제거 버튼 노출(종료·경과 카드와 대칭) | watch | 완료 |
| 59 | 편의성 | J5-Q2 | 입찰 불가한 기일 경과 물건이 관심함 최상단 — 백로그 12 후순위 규칙이 관심함 비교자에 미적용 | 관심함 정렬에 dday<0 후순위 규칙 재사용 | watch | 완료 |
| 60 | 편의성 | J5-Q2 | 관심함 제거가 확인·되돌리기 없이 즉시 영구 삭제(오탭 복구 불가) | 제거 후 되돌리기 1회 제공 | watch | 완료 |
| 61 | 편의성 | J5-Q2 | 빈 관심함 카드가 래퍼 p-4 + EmptyState m-4 이중 여백으로 타 화면 대비 32px 좁게 렌더 | 이중 여백 해소(레이어 간 패딩 중복 금지 규칙) | watch | 완료 |
| 62 | 편의성 | J5-Q3 | "기일 변경" 배지의 날짜만 formatDateKr을 경유하지 않아 요일 없는 원시 ISO로 표기 | format.ts 단일 경유 규칙 적용 | watch | 완료 |
| 63 | 편의성 | J5-Q4 | 같은 오류 상황인데 관심함만 습니다체 — 앱 문체가 화면마다 갈림 | 단정형으로 통일 + 게이트 문체 룰 연동(49번과 동반) | watch | 완료 |
| 64 | 편의성 | J6-Q2 | 관심 물건의 재유찰·기일 변경이 재방문 첫 화면·탭 어디에도 안 드러나 관심함을 직접 열어야 앎 | 하단 탭 관심함에 변화 건수 배지 | watch | 완료 |
| 65 | 편의성 | J6-Q5 | 오프라인에서 미캐시 상세가 무반응 실패하고 오프라인 상태 자체를 고지하지 않음 | 오프라인 폴백 화면 + 온라인 복귀 감지 안내 | pwa | 완료 |
| 66 | 편의성 | J6-Q5 | PWA 설치 아이콘 미완비 — maskable 아이콘도 apple-touch-icon도 없음 | gen-icons로 maskable·apple-touch-icon 생성 + manifest·layout 반영 | pwa | 완료 |
| 67 | 편의성 | J7-Q2 | 상세 "뒤로" 폴백 판정(history.length>1)이 앱 내 이력을 반영 못해 외부 유입자가 앱 밖으로 이탈 | 앱 내 내비게이션 플래그(sessionStorage)로 폴백 판정 교체 | exit-shell | 완료 |
| 68 | 편의성 | J1-Q1 | 데이터 로드 실패를 기준일 바와 본문 ErrorState가 2중 안내(1정보 1표시 위반) | 실패 안내는 본문 1곳으로, 기준일 바는 값 미표기만 | data | 완료 |
| 69 | 편의성 | J7-Q2 | 이탈·종료 확인 수단 부재 — 저장 없이 떠나면 탐색 결과가 전부 소멸(감사에서 부재 확인) | 홈 뒤로가기 확인 시트 + 조건부 beforeunload(N2) | exit-shell | 완료 |
| 70 | 접근성 | J1-Q4 | 온보딩 열림 중 설치 배너만 inert 미적용 — 배너가 effect로 늦게 마운트돼 배경 비활성 계약(백로그 4) 붕괴 | inert 적용을 마운트 시점 무관하게(속성 기반) 재구성 | exit-shell | 완료 |
| 71 | 접근성 | J1-Q4 | 최초 화면 텍스트 6곳 대비 4.5:1 미달(필터 보조 3.12·온보딩 보조 4.19·로딩 라벨 3.65) | 해당 토큰 상향(1차 라운드 9·10과 동일 처방을 누락 지점에 적용) | a11y-global | 완료 |
| 72 | 접근성 | J2-Q4 | 온보딩 시트 텍스트 2곳 4.19:1 — 1차 9·10에서 상세·관심함만 상향하고 시트를 누락 | text-ink/60→/70 | onboarding | 완료 |
| 73 | 접근성 | J1-Q4 | 홈에 h1이 없어 제목 개요가 성립하지 않음 | 홈 h1 신설(시각 규격 유지) | a11y-global | 완료 |
| 74 | 접근성 | J3-Q4 | /guide에 h1 부재 + section aria-label과 h2가 동일 문자열이라 이중 낭독 | h1 신설 + 중복 aria-label 제거 | me-screen | 완료 |
| 75 | 접근성 | J1-Q4 | 헤더 워드마크(전 화면 유일 홈 백링크) 터치타깃 124×28px — 44px 미달 | 헤더 링크 히트영역 44px 확보 | exit-shell | 완료 |
| 76 | 접근성 | J1-Q4 | 본문 글꼴이 px 고정이라 사용자 브라우저·OS 글꼴 크기 설정이 전혀 반영 안 됨 | 본문 타이포를 rem 기반으로 전환(디자인 토큰 경유) | a11y-global | **보류** — 전역 타이포 rem 전환은 41개 UI 파일의 px 리터럴을 일괄 바꿔야 해 세로 리듬 실측치(홈 1127px 등)가 전부 무효화된다. 재개 조건: MASTER.md에 rem 스케일 토큰을 먼저 확정하고 세로 리듬 기준값을 재측정한 뒤 단독 라운드로. |
| 77 | 접근성 | J1-Q4 | 고대비(forced-colors) 모드에서 선택 칩·픽 배지 상태색이 붕괴 | `@media (forced-colors: active)` 규칙 추가 | a11y-global | 완료 |
| 78 | 접근성 | J1-Q4 | 주 CTA 도달에 Tab 36회, 건너뛸 수단 0개 | 본문 바로가기(skip link) 추가 | a11y-global | 완료 |
| 79 | 접근성 | J2-Q4 | 시트에 aria-describedby 부재 + 초기 포커스가 첫 지역 칩 — 브랜드·픽 기준 설명을 낭독 없이 건너뜀 | aria-describedby 연결 + 초기 포커스를 제목으로 | onboarding | 완료 |
| 80 | 접근성 | J3-Q4 | 관심조건 비선택 칩이 흰 카드 위 흰 배경 + 테두리 1.23:1 — 컨트롤 경계 식별 불가(WCAG 1.4.11) | 비선택 칩 테두리 대비 3:1 이상으로 | me-screen | 완료 |
| 81 | 접근성 | J3-Q4 | 칩 묶음이 h3와 프로그래매틱 미연결 — 홈 필터(fieldset+legend)와 달리 소속을 알리지 못함 | fieldset+legend 또는 group+aria-labelledby로 통일 | me-screen | 완료 |
| 82 | 접근성 | J4-Q4 | 상세 외부 링크 3종이 target=_blank인데 새 창 열림 고지가 접근 이름·시각 어디에도 없음 | 접근 이름에 "새 창" 병기 | detail-link | 완료 |
| 83 | 접근성 | J4-Q4 | 리스트 더보기 카운트 대비 4.25:1 | 토큰 상향 | list-nav | 완료 |
| 84 | 접근성 | J4-Q4 | 카드 접근 이름에 두 금액의 의미 라벨이 없어 감정가·최저가 구별 불가 | sr-only 라벨 병기 | list-nav | 완료 |
| 85 | 접근성 | J4-Q4 | 재매각 사건 보증금 문구가 "재매각"을 두 번 말하고 2문장으로 늘어짐 | 1문장 압축·중복 제거 | detail-link | 완료 |
| 86 | 접근성 | J6-Q4 | 기준일 바 로드 실패 문구가 role=alert 없이 조용히 표시되고 복구 동선도 없음 | 39·68번과 함께 처리 | data | 완료 |
| 87 | 성능 | J1-Q5 | 4G 모사에서 결과 건수 표시가 2.84초 지연 — 전송 471KB 중 폰트 262KB(10파일)가 대역 선점 | Pretendard 서브셋 축소·preload 범위 축소로 데이터 요청 선행 | a11y-global | **보류** — 폰트 262KB 축소는 Pretendard 서브셋 재생성이 필요하고 한글 글리프 누락 시 전 화면이 깨진다. 재개 조건: 실사용 글리프 집합을 산출해 서브셋을 만들고 17개 화면 시각 회귀 대조 후. |
| 88 | 성능 | J3-Q5 | 저장된 관심조건이 진입 후 212ms 동안 전부 "미선택"으로 표시 | 하이드레이션 전 상태 반영 또는 스켈레톤 처리 | me-screen | 완료 |
| 89 | 성능 | J4-Q5 | 목록 끝까지 스크롤 시 상세 RSC 프리페치 119건(약 2.4MB) 발생 | 카드 링크 prefetch 비활성 또는 뷰포트 상한 적용 | list-nav | **보류** — prefetch 비활성은 상세 진입 139ms(백로그 29 성과)를 되돌린다. 재개 조건: 뷰포트 상한 prefetch를 구현해 두 지표를 함께 측정한 뒤 결정. |
| 90 | 정합성 | J4-Q3 | **기지 A** — 목데이터 detailUrl이 전부 courtauction.go.kr 루트로 고정(크롤러 DETAIL_URL 규격과 불일치) | gen-mock·전 지역 JSON을 DETAIL_URL 규격으로 통일(N3) | detail-link | 완료 |
| 91 | 불안정 | J6-Q3 | **기지 B** — `scripts/crawl.ts` 미커밋으로 주간 갱신 파이프라인 파손(Actions에서 npm run crawl 실패) | crawl.ts 커밋 + `--dry-run` 실검증(N4) | data | 완료 |
| 92 | 정합성 | J6-Q3 | **기지 C** — 다음 갱신 예정일이 지났는데 지연 표기 없음 | 기준일 바에 갱신 지연 표기(N4) | data | 완료 |
| 93 | 편의성 | J7-Q5 | **기지 D** — Phase 4 미커밋 + 설치 배너가 결과 버튼과 겹침 | 배너 오프셋 재배치 + Phase 4 일괄 마감(N5) | exit-shell·pwa | 완료 |
